# Litematica Blueprint Assistant 软件需求规格说明书
| 文档版本  | 修改日期       | 修改人       | 修改说明                                                                               |
| ----- | ---------- | --------- | ---------------------------------------------------------------------------------- |
| V1.0  | 2026-04-10 | CharaDust | 初稿创建，按参考项目文档结构编写                                                                   |
| V1.1  | 2026-04-11 | CharaDust | 补充属性页区域列表（Line.5）设计与 SNBT 映射                                                       |
| V1.2  | 2026-04-11 | CharaDust | 补充旧项目（`script/`）统计指标：红石偏度、材质、液体偏度、密度算法                                             |
| V1.3  | 2026-04-12 | CharaDust | 渲染方案：以 Deepslate（内嵌 WebView）为主路径，`legacy` 为备选并持续迭代；新增 **2.6.3** 技术说明               |
| V1.4  | 2026-04-12 | CharaDust | 明确 Deepslate 非每次启动从 GitHub 拉代码；选项页增加渲染组件更新相关项（**2.0.4**、**2.6.3.9**）               |
| V1.5  | 2026-04-13 | CharaDust | Render「完整入镜导出」参数配置化：新增选项项与默认值提示，区分透视/正交公式并写入设置（**2.0.4**、**2.6.3.6**、**2.6.3.10**） |
| V1.6  | 2026-05-06 | Codex     | 新增“枚举器”需求：Minecraft 数据值全集、命名集合、复制、集合计算与跨模块复用（**2.0.2**、**2.10**、**4.3**、**8.1**）   |
| V1.7  | 2026-05-17 | Codex     | 补充旧项目资源管理/消费模型、Nova 当前资源消费模式与迁移层映射（**2.0.4**、**4.4**、**5.3**）                      |
| V1.8  | 2026-05-18 | Codex     | 明确枚举器首批基础全集的数据来源、提取规则、落盘目录与删除约束（**2.10**、**4.3**）                                  |
| V1.9  | 2026-05-20 | Codex     | 设计统计页“枚举器信息”自定义显示区：支持理论版本、布尔判定、占比计算及行内表达式编辑（**2.4.3**）                             |
| V1.10 | 2026-5-29  | CharaDust | 补充各个模块的UI布局                                                                        |
| V1.11 | 2026-07-23 | CharaDust | 重新设计替换模块：定义替换单元概念，明确输入/输出端条目结构、权重机制与方块选择入口（**2.7**）                              |

---
## 1. 项目概述
### 1.1 项目名称
**Litematica Blueprint Assistant（简称：Litematica BA）**
### 1.2 项目定位
一款面向 Minecraft `*.litematic` 投影文件的本地管理与分析工具，目标是在不启动游戏的前提下完成文件管理、属性读写、统计分析、分层展示与渲染输出。
### 1.3 名称释义
- **BA**：Blueprint Assistant 的缩写
- **BA（影射）**：同时影射“蔚蓝档案（Blue Archive）”首字母
### 1.4 易混淆术语声明
- **预览图**：文件内 `PreviewImageData` 数组表示的图像
- **缩略图**：本软件渲染得到的 PNG 图像（亦称渲染图）
### 1.5 核心价值
1. **离线管理**：无需启动游戏即可管理投影文件
2. **统一入口**：以“激活文件”驱动各模块协同
3. **可视化能力**：支持预览图处理、区域渲染、分层展示与材料统计
4. **集合化能力**：将 Minecraft 数据值整理为可复制、可计算、可复用的集合，服务替换、筛选与材料判定
---
## 2. 功能性需求
### 2.0 全局主界面与导航（跨模块）
#### 2.0.1 功能描述
应用采用 **左侧导航 + 右侧主内容区** 布局，默认进入 **主页（Home）**。
#### 2.0.2 导航入口
| 顺位（自上而下） | 入口 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 主页（Home） | 当前仅显示 Logo | P2 |
| 2 | 投影库（Schematics Library） | 文件管理 | P2 |
| 3 | 属性（Properties） | 数据读写主入口 | P0 |
| 4 | 统计（Statistics） | 统计分析 | P1 |
| 5 | 分层（Flake） | 层级展示 | P2 |
| 6 | 渲染（Render） | 区域渲染 | P2 |
| 7 | 枚举器（Enumerator） | Minecraft 数据值全集、集合与映射工具 | P1 |
| 8 | 替换（Replace） | 方块替换 | P2 |
| 底部锚定 | 选项（Options） | 设置项入口 | P0 |
#### 2.0.3 跨模块规则
| 项 | 说明 | 优先级 |
|----|------|--------|
| 默认页 | 启动显示 Home | P0 |
| 导航切换 | 点击左侧入口切换右侧页面，更新激活态 | P0 |
| 活动文件 | “属性”中激活文件作为其他模块默认活动文件 | P0 |
| 切换保护 | 切换激活文件时若未保存，弹窗确认 | P0 |
#### 2.0.4 选项页（Settings / Options）
| 编号 | 功能点 | 描述 | 优先级 |
|------|--------|------|--------|
| FR-O.1 | 界面与主题 | 主题等通用外观设置 | P0 |
| FR-O.2 | 调试与侧栏 | UI 测试入口、控件信息、磁贴网格等 | P1 |
| FR-O.3 | Deepslate 更新说明 | 选项内说明：**Deepslate 在构建时打包，启动默认不从 GitHub 下载源码** | P1 |
| FR-O.4 | 启动时检查渲染组件 | 开关项 `deepslate_check_updates_on_startup`（默认关）；在**独立渲染资源包更新源**就绪后，可在启动时联网检查（与 npm/GitHub 源码拉取无关） | P2 |
| FR-O.5 | 手动检查渲染组件 | 按钮「立即检查渲染组件更新」：当前版本提示策略与完整应用升级路径；将来可改为拉取清单并下载差分包 | P2 |
| FR-O.6 | NBT 完整入镜导出参数 | 提供「NBT 3D — 完整入镜导出（导出…）」参数组；参数持久化到 `settings.json`，控件提示需注明内置默认值；参数调整后对后续导出立即生效 | P1 |
| FR-O.7 | 游戏资源管理 | 提供游戏语言、方块图标、物品图标与版本化游戏数据的管理入口；支持查看当前生效资源、下载/导入/删除资源、切换激活状态、执行资源健康检查与触发前端缓存失效/重载 | P1 |

---
### 2.1 模块一：主页（Home）
#### 2.1.1 功能描述
当前版本主页仅显示项目 Logo。
#### 2.1.2 功能点
| 编号 | 功能点 | 描述 | 优先级 |
|------|--------|------|--------|
| FR-H.1 | 首页展示 | 显示 Logo 与基础欢迎布局 | P2 |

---
### 2.2 模块二：文件管理（投影库 / Schematics Library）
#### 2.2.1 功能描述
在指定路径统一管理投影文件，支持多个目录聚合（类似 Windows“库”）。
#### 2.2.2 路径与设置
| 项 | 说明 | 默认值/建议 | 优先级 |
|----|------|-------------|--------|
| 主库路径 | 库根目录 + 默认保存位置 | 开发：项目根 `data/schematics/`；生产：EXE 同级 `data/schematics/` | P0 |
| 多文件夹库 | 支持挂载多个文件夹 | 开启 | P0 |
| 子目录展开 | 目录树默认“全部展开到最内层文件”，可切换“全部关闭” | 可配置 | P1 |
#### 2.2.3 文件交互
| 项      | 说明                                 | 优先级 |
| ------ | ---------------------------------- | --- |
| 顶部按钮   | 重命名、移动到回收站、发送到文件夹（库内目录）            | P0  |
| 激活文件   | 双击 `.litematic` 激活；提供激活按钮          | P0  |
| 未保存确认  | 激活切换前若有未保存内容需确认                    | P0  |
| 悬浮预览   | 悬浮文件时显示缩略图（若存在）                    | P1  |
| 异步预览读取 | `PreviewImageData` 位于二进制文件内，后台异步加载 | P0  |
#### 2.2.4 界面与字段
##### 主界面字段

| 区块     | 功能项           | 说明                                   | 优先级 |
| ------ | ------------- | ------------------------------------ | --- |
| Title  | 标题            | “投影库”                                | P0  |
| Line.1 | 【添加本地记录...】   | 选择本地文件添加到库中记录                        | P0  |
| Line.1 | 【从在线投影库获取...】 | 打开在线投影库窗口                            | P0  |
| Line.1 | 【管理本地库文件夹...】 | 打开本地库文件夹窗口，具体功能之后再议论                 | P1  |
| Line.1 | 【刷新】          |                                      | P0  |
| Line.2 | 搜索框           |                                      | P0  |
| Line.2 | 标签筛选          | 仅列出从记录中有的标签                          | P2  |
| Line.2 | 状态筛选          | 状态包括：正常、文件丢失、解析失败                    | P0  |
| Line.2 | 排序方式          |                                      | P0  |
| Line.3 | 内容区           | 在搜索框无内容时，显示“最近内容”；搜索框有内容时，从所有库文件夹中搜索 | P0  |
| 页脚     | 数量统计          | 统计内容区包含多少记录，多少条需要关注                  | P0  |
| 页脚     | 每页条目数         | 控制每页多少个条目                            | P0  |
| 页脚     | 翻页器           | 【上一页】第x页，共x页【下一页】，其中可以直接输入数字直接跳转     | P0  |
##### 内容物字段
| 区块       | 功能项         | 说明                                     | 优先级 |
| -------- | ----------- | -------------------------------------- | --- |
| Line.1   | 名称          | 加粗显示                                   | P0  |
| Line.1   | 状态          | 正常状态不显示，“文件丢失”，“解析失败”分别显示橙色、红色的文本在名称后面 | P0  |
| Line.1R  | 文件大小        | 显示在这一行的右侧                              | P0  |
| Line.2   | 标签          |                                        | P0  |
| Line.3   | 内部名称        |                                        | P0  |
| Line.4   | 作者          |                                        | P0  |
| Line.5   | 文件名         |                                        | P0  |
| Line.6   | 文件路径        |                                        | P0  |
| Line.7   | 时间          |                                        | P0  |
| Line.8   | 【激活】        | 按钮，点击以激活文件                             | P0  |
| Line.8   | 【属性】        | 按钮，点击激活并跳转到属性                          | P0  |
| Line.8   | 【发送...】     | 按钮，点击则将投影发送到其他库，不激活                    | P0  |
| Line.8   | 【打开目录】      | 按钮，点击打开所在文件夹，不激活                       | P0  |
| Line.8   | 【删除记录】      | 按钮，点击删除记录，不激活                          | P0  |
| 右侧Line.1 | 渲染图/预览图     | 显示渲染图/预览图区域，关于预览图的定义参考1.4混淆术语          | P0  |
| 右侧Line.2 | 【切换渲染图/预览图】 | 按钮，点击即可切换渲染图和预览图                       | P0  |
| 右侧Line.3 | 【生成渲染】      | 按钮，点击生成渲染图                             | P0  |
##### 在线库列表字段

| 区块      | 功能项   | 说明                    | 优先级 |
| ------- | ----- | --------------------- | --- |
| Line.1  | 名称    | 加粗显示                  | P0  |
| Line.2  | 标签    |                       | P0  |
| Line.3L | 作者    |                       | P0  |
| Line.3R | 下载/收藏 |                       | P0  |
| Line.4  | 状态    | 当前固定显示“在线可导入”，不压缩进前三行 | P0  |

在线投影库的左侧结果列表应为独立滚动区域：当结果过多时，仅左侧列表区出现滚动条并单独滚动；窗口整体页面不应因结果增多而向下滚动。


---
### 2.3 模块三：数据读写（属性 / Properties）
#### 2.3.1 功能描述
读写投影文件头部元数据（SNBT），当前以 `version=5` 为主要目标版本。
#### 2.3.2 界面与字段
| 区块      | 功能项          | UI组   | 说明                                   | 优先级   |
| ------- | ------------ | ----- | ------------------------------------ | ----- |
| Line.1  | 选择文件...      |       | 选择库外文件并激活；激活前执行未保存确认                 | P0    |
| Line.1  | 在库中选择...     |       | 跳转至投影库选择并激活文件                        | P0    |
| Line.2  | 文件名称         | 文件    | 可编辑                                  | P0    |
| Line.3L | 作者           | 元数据   | 对应 `Author`                          | P0    |
| Line.3L | 描述           | 元数据   | 对应 `Description`                     | P0    |
| Line.3L | 时间           | 元数据   | 创建/修改时间只读                            | P0    |
| Line.3L | 尺寸           | 元数据   | `EnclosingSize(x,y,z)` 只读            | P0    |
| Line.3L | 体积           | 元数据   | `TotalBlocks`、`TotalVolume` 只读       | P0    |
| Line.3R | 预览图画布        | 预览/渲染 | 根据 `PreviewImageData` 显示（140x140 倍数） | P0    |
| Line.3R | 【清空预览图】      | 预览/渲染 | 按钮，清空预览图数组                           | P0    |
| Line.3R | 【导入预览图】      | 预览/渲染 | 按钮，导入图片，裁剪为正方形后缩放至 140x140，可选采样方式    | P0    |
| Line.3R | 渲染图画布        | 预览/渲染 | 存放渲染图cache，复用投影库中的渲染图功能，共享cache      | P1    |
| Line.3R | 【生成渲染】       | 预览/渲染 | 按钮，生成渲染图，复用投影库中的生成渲染图功能              | P1    |
| Line.3R | 【打开弹窗Viewer】 | 预览/渲染 |                                      |       |
| Line.4  | 版本信息         |       | 投影文件版本、Minecraft 数据版本只读              | P0    |
| Line.5  | 区域列表         | 区域列表  | 以表格列出全部子区域；列定义见 **2.3.4**            | P0    |
| 页脚      | 操作按钮         |       | 保存、另存为、恢复默认值、转换格式（非优先）               | P0/P2 |

**恢复默认值：** 每次成功从磁盘加载（含「选择文件…」与「另存为」后对新路径的加载）时，在内存中保留一份当时的元数据快照。用户编辑后点击「恢复默认值」时，将表单与内存模型还原为该快照（与刚读入文件时一致），**不是**清空字段。保存到原文件不会刷新该快照；若需以磁盘最新内容为基准，应重新打开文件。快照在切换活动文件（再次加载）时由新文件内容覆盖。

#### 2.3.3 SNBT 映射（version=5）
```SNBT
Author: "string",
Description: "string",
EnclosingSize: {
  x: ,
  y: ,
  z:
},
TotalBlocks: ,
TotalVolume: ,
PreviewImageData: [],
Regions: {
  区域名: {
    Size: { x: , y: , z: },
    Position: { x: , y: , z: }
  }
}
```
其中 `Regions` 为复合标签，键名即子区域在文件中的标识；每个子区域下含 `Size`、`Position` 等（完整子树以解析层为准，属性页本列表仅约束上述列的展示与编辑范围）。

#### 2.3.4 区域列表（Line.5）
##### 2.3.4.1 功能描述
在版本信息（Line.4）与页脚操作按钮之间，以**表格**展示当前投影内全部子区域的一览：只读展示各区域的尺寸与相对位置；允许用户修改**区域名称**（即 SNBT 中 `Regions` 下的子复合键名），保存时写回文件。

##### 2.3.4.2 布局与表头
采用**两级表头**（或等价 UI：分组列标题 + 子列标题），共 **7 列数据**：

| 分组表头 | 子表头 / 列 | 说明 | 可编辑 |
|----------|-------------|------|--------|
| 区域名称 | （单列） | 对应 SNBT 中 `Regions` 的子键名，即 `Regions.<区域名>` 里的 `<区域名>` | 是 |
| 区域尺寸 | x、y、z | 对应 `Regions.<区域名>.Size.x` / `Size.y` / `Size.z` | 否 |
| 相对位置 | x、y、z | 对应 `Regions.<区域名>.Position.x` / `Position.y` / `Position.z` | 否 |

- 每一行对应 `Regions` 下的一个子区域；行顺序可按区域名排序或按文件内原始顺序展示（实现阶段二选一或提供设置项，首版建议与解析层迭代顺序一致）。
- 若无子区域，表格为空，可显示空状态提示（文案走 i18n）。

##### 2.3.4.3 数据与保存语义
| 项 | 说明 | 优先级 |
|----|------|--------|
| 加载 | 打开/激活文件后，与元数据一并解析 `Regions`，填充表格 | P0 |
| 重命名 | 用户修改「区域名称」时，在内存模型中将该子复合从旧键迁移为新键，**子树内容（含方块数据等）保持不变**；新键不得与已有区域重名、不得为空 | P0 |
| 只读列 | `Size` / `Position` 由文件几何信息决定，不在本表直接编辑；若未来在其他功能中修改区域边界，保存后需重新加载以刷新本表 | P0 |
| 恢复默认值 | 「恢复默认值」将区域列表一并还原为加载时的快照（含区域名与只读列） | P0 |
| 版本差异 | 非 `version=5` 时，`Regions` 结构可能不同，解析层应分支处理；本表在无法解析时降级为只读提示或隐藏列（与全局版本策略一致） | P1 |

##### 2.3.4.4 与其他模块的关系
- **材料列表 / 统计 / 分层 / 渲染** 中的「选定区域」下拉或语义，应以 `Regions` 的键名为稳定标识；属性页重命名保存后，这些模块在下次读取活动文件时应使用新名称。
- 本列表**不**承担区域增删、方块编辑；仅覆盖名称修改与几何只读展示。

#### 2.3.5 分阶段 SNBT 读取策略
为解决大文件加载时 UI 未响应问题，投影文件 SNBT 解析采用**三阶段分层读取**策略，按需逐步加载，优先呈现头部元数据与区域列表，延迟加载体积庞大的方块数据。

**阶段一：头部元数据（优先）**
- 读取除 `Regions` 外的所有 SNBT 字段：
  - `Author`、`Description`、`EnclosingSize`、`TotalBlocks`、`TotalVolume`
  - `PreviewImageData`、`TimeCreated`、`TimeModified`
  - `Version`、`MinecraftDataVersion`
- 该阶段数据量小，可立即填充属性页表单，用户可立即查看/编辑基础信息。

**阶段二：区域几何信息（次优先）**
- 遍历 `Regions` 复合标签，仅读取每个子区域的：
  - 区域名（`Regions` 的子键名）
  - `Position`：`{ x, y, z }`
  - `Size`：`{ x, y, z }`
- 该阶段不读取 `Regions.*.BlockStates`、`Regions.*.Entities` 等大数据字段。
- 读取完成后填充属性页「区域列表」表格，用户可查看区域名称、尺寸与位置，支持重命名操作。

**阶段三：区域详细数据（按需/后台）**
- 当需要时才读取 `Regions` 内的其他内容：
  - `BlockStates`：方块状态数组（体积最大）
  - `Entities`：实体列表
  - `PendingBlockTicks`、`PendingFluidTicks` 等
- 该阶段数据用于：材料统计、渲染、分层展示、方块替换等后续功能模块。
- 建议在后台线程异步加载，配合进度指示器，避免阻塞 UI。

**设计收益**

| 场景 | 效果 |
|------|------|
| 仅查看/编辑元数据 | 无需加载方块数据，大文件也能秒开 |
| 仅查看区域列表 | 快速获取区域名与几何信息，支持重命名保存 |
| 统计/渲染 | 按需加载目标区域的 BlockStates，减少内存占用 |
| 保存操作 | 若未修改区域名且未涉及方块数据，可跳过阶段三直接写回 |

---
### 2.4 模块四：统计分析（Statistics）
#### 2.4.1 功能描述
沿用旧项目统计逻辑实现红石偏度、材质、液体偏度、密度等结果展示。
#### 2.4.2 功能点
| 编号 | 功能点 | 描述 | 优先级 |
|------|--------|------|--------|
| FR-S.1 | 材料列表入口 | “材料列表”按钮打开独立窗口 | P0 |
| FR-S.2 | 统计指标 | 使用旧逻辑计算偏度/密度相关数据 | P1 |
| FR-S.3 | 缓存优先 | 若有缓存先展示灰色缓存内容 | P0 |
| FR-S.4 | 异步刷新 | 后台完成统计后更新显示 | P0 |
| FR-S.5 | 图表区域 | 图表同样采用缓存优先 + 异步更新 | P1 |
#### 2.4.3 界面与字段

| 区块        | 功能项        | UI组   | 说明                                   | 优先级 |
| --------- | ---------- | ----- | ------------------------------------ | --- |
| Line.1    | 【材料列表】     |       | 按钮，用于打开“材料列表”子窗口                     |     |
| Line.1    | 【重新统计】     |       | 按钮，用于刷新数据                            |     |
| Line.1    | 【打开枚举器...】 |       | 按钮，用于打开“枚举器”子窗口                      | P2  |
| Line.2    | 统计容器       |       | 复选框，用于切换统计容器开关                       | P1  |
| Line.2    | 统计实体       |       | 复选框，用于切换统计实体开关                       | P1  |
| Line.3L   | 网格数        | 统计学信息 |                                      |     |
| Line.3L   | 方块数        | 统计学信息 |                                      |     |
| Line.3L   | 密度         | 统计学信息 |                                      |     |
| Line.3L   | 区域数量       | 统计学信息 |                                      |     |
| Line.3L   | 包围尺寸       | 统计学信息 |                                      |     |
| Line.3L   | 结构类型       | 统计学信息 |                                      |     |
| Line.3L   | 红石偏度       | 统计学信息 |                                      |     |
| Line.3L   | 液体偏度       | 统计学信息 |                                      |     |
| Line.3L   | 实体种类       | 统计学信息 |                                      |     |
| Line.3R.1 | 主要材料       | 主要材料  | 列出排名前5的材料及其占比                        |     |
| Line.3R.2 | 枚举器信息      | 枚举器信息 | 按用户配置的枚举表达式显示统计结果，支持版本、布尔值、占比等类型     | P2  |
| Line.3R.2 | 【编辑信息框】    | 枚举器信息 | 按钮，用于打开“枚举统计编辑器”对话框并编辑显示规则           | P2  |
| Line.3R.2 | 信息项列表      | 枚举器信息 | 多行键值显示区；每行由“标题 + 表达式结果”组成，按编辑器中的顺序渲染 | P2  |

#### 2.4.3.1 枚举器信息区
“枚举器信息”位于统计页面右侧，与“主要材料”并列，面向已经完成材料统计的当前活动文件。该区域不写死字段，而是由用户通过简单表达式配置若干显示行。

约束：
- 数据源为当前统计结果中的**材料列表计数字典**，而不是只读集合大小。
- 集合运算复用枚举器模块的集合语义，默认以当前材料列表对应集合 `L` 作为输入。
- 每一行输出一种最终结果，首版支持：**理论版本**、**布尔判定**、**占比**。
- 结果区仅负责显示，不直接编辑集合内容；集合本身仍在“枚举器”模块维护。

建议默认预置 3 行：

```text
理论版本 = version(L)
包含生存不可达方块 = any(L ∩ survival_impossible)
木头占比 = ratio(L ∩ 木头)
```

#### 2.4.3.2 枚举统计编辑器
点击【编辑信息框】后打开“枚举统计编辑器”对话框，用于编辑“枚举器信息”显示规则。

| 区块 | 功能项 | 说明 | 优先级 |
|------|--------|------|--------|
| Line.1 | 多行编辑框 | 按行编辑规则；空行忽略；支持粘贴多行配置 | P2 |
| Line.2L | 语法提示区 | 展示可用函数、集合运算符与示例 | P2 |
| Line.2R | 实时预览区 | 基于当前活动文件的统计结果预览每一行最终显示值 | P2 |
| Line.3L | 错误提示 | 指出行号、错误原因、不可解析的集合名/函数名 | P2 |
| Line.3R | 【恢复默认】 | 恢复内置的默认三行配置 | P2 |
| Line.3R | 【保存】 | 保存当前规则；保存前必须通过语法校验 | P2 |

交互规则：
- 一行只定义一个显示项，基本格式为 `标题 = 表达式`。
- 标题用于统计页左侧标签显示，表达式结果显示在右侧。
- 保存时若任一行解析失败，则阻止保存并定位到对应行。
- 预览使用当前活动文件的最新统计结果；若当前尚未完成统计，则预览区显示“等待统计结果”。

#### 2.4.3.3 简化语法
首版使用行级 DSL，避免直接暴露完整脚本能力。

**基础格式**

```text
显示标题 = 函数(集合表达式)
```

**集合表达式**
- 复用枚举器集合表达式能力，支持集合名、`L`、括号，以及 `∪/+`、`∩/*/&`、`|`、`-`。
- 其中 `&` 作为 AND 的简写，等价于交集 `∩`；`|` 作为 OR 的简写，等价于并集 `∪`。
- `L` 固定表示当前活动文件材料列表中出现过的材料 ID 集合。
- 集合表达式的求值结果是**ID 集合**；若后续函数需要数量，则再回到材料计数字典按 ID 取数。

**内建函数**

| 函数 | 输入 | 输出 | 说明 |
|------|------|------|------|
| `version(expr)` | 集合表达式 | 文本 | 计算理论版本 |
| `any(expr)` | 集合表达式 | `True` / `False` | 判断结果集合是否非空 |
| `ratio(expr)` | 集合表达式 | 百分比文本 | 计算结果集合对应材料数量占总方块数的比例 |

**`version(expr)` 的判定规则**
1. 先计算 `expr` 得到目标集合；常见输入为 `L`。
2. 当前枚举全集 `U` 定义为**当前已启用枚举基础全集**中 `base/` 分类下所有集合的并集；实际加载来源需按 `installed.json` 解析当前生效的枚举数据根目录，而不是固定读取某个目录名。
3. 取枚举器中分类为“版本”的所有集合，与目标集合逐一做交集。
4. 仅保留交集非空的版本集合，取其中**最高版本**作为显示结果。
5. 版本集合命名规则为 `v_x_y_z`，显示时转换为 `x.y.z`。
6. 版本比较必须按版本号逐级比较，而不是按字典序比较；先比 `x`，再比 `y`，最后比 `z`。例如 `26.1 > 1.21.1 > 1.20.6`，对应 `v_26_1 > v_1_21_1 > v_1_20_6`。
7. `v_older` 视为 **1.12.2**，并作为最旧版本参与排序；该段判定与枚举器模块内既有设计保持一致。
8. 若目标集合中存在元素落在当前枚举全集 `U` 内，但**不属于任何“版本”集合**，则说明本地版本集合覆盖不完整；此时仍显示已命中的最高版本，并在其后追加 ` / 更高版本`。
9. 展示文本建议去掉前缀 `v_`，例如 `v_1_21_10` 显示为 `1.21.10`，`v_older` 显示为 `1.12.2 之前`。

**`any(expr)` 的判定规则**
- 当 `expr` 结果集合为空时显示 `False`；非空时显示 `True`。
- 典型用法：`any(L ∩ survival_impossible)`，用于判断当前材料列表是否命中某个命名集合。

**`ratio(expr)` 的计算规则**
1. 先计算 `expr` 得到目标 ID 集合，例如 `L ∩ 木头`。
2. 用该集合去筛选当前材料计数字典，只累加命中 ID 的数量，得到分子。
3. 分母为当前统计结果中的**总方块数**。
4. 最终结果为 `分子 / 总方块数 × 100`，显示为保留一位小数的百分比。
5. 若总方块数为 0，则显示 `--`，避免除零。

**示例**

```text
理论版本 = version(L)
生存不可达 = any(L ∩ survival_impossible)
木头方块占比 = ratio(L ∩ 木头)（此集合没做，先不管）
下界相关占比 = ratio(L ∩ (下界岩制品 ∪ 石英制品))（此集合没做，先不管）
```

#### 2.4.3.4 数据与落盘约束
- 该编辑器保存的是“显示规则配置”，不是集合本体；集合本体仍由枚举器模块的集合数据源负责持久化。
- 建议按用户设置落盘，例如写入 `settings.json` 中的 `statistics.enumerator_info_rules`。
- 规则应与活动文件解耦：切换文件后复用同一套规则，只替换统计输入数据。
- 当规则引用的集合已被删除或重命名时，统计页应在对应行显示错误占位，并在编辑器中给出明确提示。

#### 2.4.3.5 与枚举器模块的关系
- 统计页不维护独立集合语法，应直接复用枚举器模块的集合解析器。
- 当前统计上下文中的枚举全集 `U`，应解析当前 `installed.json` 所指向的生效枚举基础数据，并将其中 `base/` 分类下所有集合做并集后得到。
- “版本”类集合命名建议沿用 `v_...` 约定，供 `version(expr)` 统一识别。
- 材料列表导出的 `L` 只在统计页计算上下文中作为隐式输入，不要求用户先手动保存同名集合。

#### 2.4.4 旧项目统计指标算法（与 `script/LitematicaViewer.py` 对齐）
以下公式与分支与旧查看器分析流程一致，便于新实现逐字复现。配置数据来自 `lang/category.json`；分类函数为 `script/Litmatool.py` 中的 `Category_Tran`。

**符号约定**

- `num`：遍历区域内所有格子后得到的**非空气方块总数**。空气三态（`minecraft:air`、`cave_air`、`void_air`）不计入。
- `Block`：方块 ID → 出现次数的聚合字典。下列 ID 虽会计入 `num`，但**不写入** `Block`（仍写入 `Block_pos` 供渲染）：`minecraft:piston_head`、`minecraft:nether_portal`、`minecraft:moving_piston`、`minecraft:bedrock`。
- 旧项目在解析时还会做 ID 归一（如耕地→泥土、气泡柱→水）、子串替换（如含 `potted_`、`_cake` 等）、以及按方块属性拆分/合并计数（含水、双层半砖、头颅/床的一半等）；与上述规则共同决定 `Block` 的键与计数值。实体可选计入 `Block`，键形如 `E/minecraft:...`。
- `sorted_block`：将 `Block` 按计数值降序排列的列表。`sorted_block[0][1]` 为**字典内**出现次数最多的那一类方块的计数。
- `size_x`、`size_y`、`size_z`：旧代码在每个子区域循环末尾被覆盖为**当前子区域**的包围尺寸（`max* - min* + 1`）。界面上的密度等指标若未对多区域做累加，则语义上对应**最后一个遍历到的子区域**的尺寸与全局累计的 `num` 组合（实现迁移时建议明确：要么完全复现该行为，要么改为全区域总体积）。

**方块 → 材质大类（用于「红石」「容器」「液体」等桶）**

1. 对 `Block` 中每个键 `val`（实体分支见下），取资源 ID 本地名：`id = val.split("[")[0].split(":")[-1]`（去掉 SNBT 属性段与命名空间）。
2. 若 `val` 以 `E/` 开头，归入 **实体**。
3. 否则调用 `Category_Tran(id)`：将 `id` 按下划线 `_` 拆成若干段，按 `category.json` 的**对象键顺序**依次遍历每个大类及其关键词列表；若任一段字符串**等于**列表中某项，则返回该大类中文名；若无匹配返回空串，方块计入 **其他**。
4. 「红石偏度」分子用到的计数 = 大类 **红石** 中所有 `(次数, id)` 的 `次数` 之和 + 大类 **容器** 中同样之和。

**密度（界面「密度」/`a_den`）**

$$
\text{密度(\%)} = \frac{num}{size_x \cdot size_y \cdot size_z} \times 100
$$

结果按旧 UI 显示为保留一位小数的百分比字符串。

**红石偏度（界面「红石偏度」/`a_red`）**

令 $R$ = 红石类总块数 + 容器类总块数。分母：

$$
D = \begin{cases}
num - sorted\_block[0][1] & |Block| > 5 \\
num & \text{否则}
\end{cases}
$$

$$
\text{红石偏度（比例）} = \frac{R}{D}
$$

界面显示为 $\times 100$ 后保留一位小数的百分比。含义上：当方块类型超过 5 种时，从 `num` 中扣除**字典内**第一大类的块数再算红石+容器占比（基岩等未进字典的块仍留在 `num` 中，不参与“减第一大类”）。

**材质（界面「材质」/`a_redt`，由红石偏度映射的文案标签）**

仅当 `num > 10` 时按阈值映射（文案键见 `lang/zh.json` 的 `ui.stat.type1`～`type5`）；否则固定为 **方块太少**（`ui.stat.type6`）。

| 条件（红石偏度比例） | 材质标签（中文示例） |
|----------------------|----------------------|
| $> 0.5$ | 红石机器 |
| $\ge 0.3$ | 生电红石 |
| $\ge 0.1$ | 生电机器 |
| $\ge 0.01$ | 结构性机器 |
| $< 0.01$ | 建筑 |

**液体偏度（界面「液体偏度」/`a_liq`）**

令 $L$ = 大类 **液体** 中所有次数之和。

$$
\text{液体占比(\%)} = \frac{L}{num} \times 100
$$

旧 UI 第一行显示该百分比（一位小数），第二行显示液体总块数加单位后缀 `u`（如 `123u`）。

**实现引用**

- 统计主流程：`script/LitematicaViewer.py` 中 `start_analysis`（`num`、`Block`、`a_den`、`redly`、`fluid`、`me_type` 等）。
- 分类：`script/Litmatool.py` 中 `Category_Tran`。
- 关键词表：`lang/category.json`（含「红石」「容器」「液体」等列表）。

---
### 2.5 模块五：层级展示（Flake）
#### 2.5.1 功能描述
沿用旧项目逻辑，实现分层平面渲染。
#### 2.5.2 功能点
| 编号      | 功能点    | 描述                                   | 优先级   |
| ------- | ------ | ------------------------------------ | ----- |
| FR-F.1  | 视图区域   | 左侧显示渲染视图                             | P1    |
| FR-F.2  | 子区域下拉  | 多子区域时可选择显示子区域                        | P1    |
| FR-F.3  | 默认区域策略 | 默认显示第一个区域；可配置默认显示全部区域                | P1    |
| FR-F.4  | 材料列表   | 右侧按钮打开当前层级约束下材料列表                    | P1    |
| FR-F.5  | 渲染方案   | **二维图像 / 平面切片** 渲染（非体素 3D），持续改进性能与画质 | P1    |
| FR-F.6  | 层级控制   | 横向单手柄滑条控制显示层级                        | P1    |
| FR-F.7  | 悬浮信息显示 | 当鼠标悬浮于方块上时，显示方块信息与状态                 | P1    |
| FR-F.9  | 选取区域   | 用于圈定矩形区域，或自由区域                       | P2/P3 |
| FR-F.8  | 矩形测量   | 给出圈定区域测量其长宽                          | P2    |
| FR-F.10 | 方块填充   | 用于填充圈定区域为方块                          | P2    |
| FR-F.11 | 方块画笔   | 用于绘制方块或清除方块                          | P2    |
| FR-F.12 | 原点放置   | 用于放置原点                               | P2    |

#### 2.5.3 界面与字段 
| 区块      | 功能项         | UI组  | 说明                                                                                       | 优先级 |
| ------- | ----------- | ---- | ---------------------------------------------------------------------------------------- | --- |
| Line.1  | 【材料列表】      |      | 按钮，打开材料列表，在此处打开材料列表时，仅显示本层的材料（工作簿切换到单层材料）                                                |     |
| Line.1  | 【生成3DCache】 |      | 按钮，用于快速生成该投影的3DCache，生成的是标准模式的Cache而非完整模式的Cache                                          |     |
| Line.1  | 编辑模式        |      | 复选框，用于切换编辑模式的开关，编辑模式尚未实现，但是保留开关功能以测试其他UI行为                                               |     |
| Line.2  | 层级          | 层级控制 | 横向滑动条，用于控制当前层级                                                                           |     |
| Line.3  | 当前层级        | 层级控制 | 显示“Y = 当前层级”                                                                             |     |
| Line.3  | 细调按钮        | 层级控制 | 排列在右侧，分别为【-5】【-】【+】【+5】                                                                  |     |
| Line.4L | 分层主界面       |      | 显示当前分层的内容，方块图标使用资源管理器中“方块图标”的2D槽，邻近采样缩放，鼠标hover时显示方块信息悬浮窗，参考材料列表中悬浮窗的样式，但是只显示图标、名称和方块状态。 |     |
| Line.4R | 编辑工具        | 编辑工具 | 编辑工具占据右侧空间，左右侧的宽度可以通过拖拽改变，目前编辑功能尚未实现                                                     |     |
|         |             |      |                                                                                          |     |
| 页脚      | 快捷栏         |      | 当编辑模式打开时底部居中显示。不随页面滚动。类似于调色板。共10个槽位，前9个放置物品，最后一个为“打开物品栏”，点击即可打开“创造模式物品栏”对话框              |     |

---
### 2.6 模块六：区域渲染（Render）
#### 2.6.1 功能描述
基于激活文件进行整体模型渲染，生成并输出缩略图。**首版以 Deepslate + 内嵌 WebView 为主渲染路径**（与社区成熟工具同源技术栈，画质与可维护性优于旧版体素渲染）；`legacy` 作为备选或离线降级方案，在后续里程碑中持续改进性能与观感。
#### 2.6.2 功能点
| 编号     | 功能点    | 描述                                                                   | 优先级 |
| ------ | ------ | -------------------------------------------------------------------- | --- |
| FR-R.1 | 视图区域   | 左侧显示模型渲染结果                                                           | P1  |
| FR-R.2 | 子区域下拉  | 多区域时支持切换显示区域                                                         | P1  |
| FR-R.3 | 默认区域策略 | 默认第一个区域；可配置默认全部区域                                                    | P1  |
| FR-R.4 | 材料列表   | 显示当前选定区域材料列表                                                         | P1  |
| FR-R.5 | 渲染方案   | **`Deepslate`（默认）**：内嵌浏览器 + WebGL；**`legacy`**：项目内旧 OpenGL 体素逻辑，持续迭代 | P1  |
| FR-R.6 | 方向控制   | 9 按钮方向控制（顶视 + 东南西北 + 4 个 45 度俯视）                                     | P1  |
| FR-R.7 | 输出按钮   | 写入预览图、导出缩略图                                                          | P1  |
#### 2.6.3 渲染技术方案：Deepslate（主路径）
本节约定宿主（Python/Qt）与前端（打包后的 JS）如何协同，便于实现与验收对齐社区实践（如 [misode/vscode-nbt](https://github.com/misode/vscode-nbt)）。

##### 2.6.3.1 技术栈与职责划分
| 层级 | 选型 | 职责 |
|------|------|------|
| 渲染核心 | [Deepslate](https://www.npmjs.com/package/deepslate)（版本在构建中锁定） | NBT/结构抽象、`Structure` / `StructureProvider`、`StructureRenderer`、方块模型与纹理图集消费（WebGL） |
| 数学库 | `gl-matrix` | 视图矩阵、投影矩阵；与 Deepslate 及 vscode-nbt 用法一致 |
| 宿主 UI | Qt `QWebEngineView`（或同等级 WebView2/WebKit 封装） | 加载本地静态页、安全策略、与业务层桥接 |
| 备选 | `legacy`（`script/LitRender.py` 等迁移路径） | 无可用 GPU/WebView、用户显式选择或开发调试时的回退 |

##### 2.6.3.2 前端资产与构建
- 将 **Deepslate**、**gl-matrix** 与**薄封装**（`.litematic` → 与 Deepslate 兼容的结构对象，逻辑可对齐 vscode-nbt 的 `Schematics.ts` / `litematicToStructure`）通过 **Rollup / Vite** 等打包为静态资源（如 `resources/renderer/`），随安装包分发。
- **不在运行时依赖** npm 或 VS Code；仅使用打包后的 JS 与资源文件。

##### 2.6.3.3 Minecraft 资源与数据版本
- Deepslate 绘制依赖 **方块状态（blockstates）**、**模型（models）**、**纹理图集**及 **UV 映射**，须与投影文件的 **Minecraft 数据版本**（属性页只读字段）一致或兼容。
- 建议缓存目录：`data/render_assets/<数据版本>/`（或等价结构），可借鉴 vscode-nbt 的下载/缓存思路；**缺失资源时**应提示用户并允许回退 `legacy`，而非静默错模。

##### 2.6.3.4 宿主 ↔ WebView 数据通道
| 方式 | 适用 | 说明 |
|------|------|------|
| 路径 + 本地只读打开 | 开发期、受控环境 | 将激活文件绝对路径传入 JS；需在 WebView 中允许访问本地文件或使用自定义 scheme，注意 Windows 路径与打包路径 |
| 二进制 / JSON 注入 | 推荐生产默认 | 宿主读取 `.litematic`，经 **`QWebChannel`**、`runJavaScript` 或 `postMessage` 将 **ArrayBuffer** 或 **NBT 树 JSON** 传入页面，由 Deepslate 侧解析为 `NbtFile` / `Structure`，避免前端任意读盘 |
| 子区域切换 | 与 FR-R.2 对齐 | 可只传当前子区域对应 NBT 子树或预裁剪结构，降低单次 GPU/内存压力 |

##### 2.6.3.5 相机预设与 FR-R.6
- 预设基于 `StructureEditor` 轨道参数定义：`cRot[0]=yaw`（绕 Y）、`cRot[1]=pitch`（绕 X），视图矩阵顺序为 `T(0,0,-cDist) * Rx(cRot[1]) * Ry(cRot[0]) * T(cPos)`；绘制循环调用 Deepslate 的 `StructureRenderer` 入口（思路同 vscode-nbt `StructureEditor`）。
- 9 个方向按钮与预设一一绑定；按钮为**瞬时触发**而非“保持选中态”，用户手动拖拽改变相机后可再次点击同一方向，强制重新应用该预设。
- NBT 3D 在**首次加载**与**重新加载 3D**完成后，统一恢复默认轨道相机（与 `StructureEditor` 初始化一致）：`cRot=(0.4, 0.6)`，并按 `onInit` 规则重算 `cPos/cDist`；FOV 仍由 FOV 滑块值决定。
- 交互式旋转/缩放可在页内实现；导出缩略图时使用与按钮一致的预设矩阵以保证输出稳定、可复现。

预设角（弧度）定义：

| 方向 | `cRot[0]`（yaw） | `cRot[1]`（pitch） |
|---|---|---|
| 顶视 | `0` | `π/2` |
| 北 | `π` | `0` |
| 南 | `0` | `0` |
| 西 | `π/2` | `0` |
| 东 | `-π/2` | `0` |
| 西北 | `atan2(sx, -sz)` | `atan2(sy, hypot(sx, sz))` |
| 东北 | `atan2(-sx, -sz)` | `atan2(sy, hypot(sx, sz))` |
| 西南 | `atan2(sx, sz)` | `atan2(sy, hypot(sx, sz))` |
| 东南 | `atan2(-sx, sz)` | `atan2(sy, hypot(sx, sz))` |

其中 `sx/sy/sz` 为结构尺寸 `getSize()` 的三个轴向分量；四个对角方向共用同一 `pitch`，仅 `yaw` 象限不同。

##### 2.6.3.6 缩略图与预览图（FR-R.7）
- 从 WebView 内 **离屏或当前 `canvas`** 导出：`canvas.toDataURL('image/png')` 或通过桥接回传 **PNG 字节流**；宿主负责写入文件内 `PreviewImageData` 或导出到用户路径。
- 分辨率与宽高比需在文档或设置中约定（例如与属性页预览图 140×140 策略协调）。
- 当前实现将输出拆分为两种动作：**截屏**（当前视口）与**导出**（完整入镜）；两者均先给出预览，再由用户选择“保存为 PNG”或“写入预览图”。
- NBT「导出」路径不修改 `canvas` 尺寸、不调用 `resize()`，仅临时调整相机参数后在当前画布导出，避免 WebGL 资源重置导致空白图。

##### 2.6.3.7 性能、体积与超大结构
- 参考 vscode-nbt：对 **超大体积**（如超过约 $48^3$ 体素）可 **警告后继续** 或 **关闭高成本特性**（如不可见块相关 Pass）；本软件可额外采用 **仅加载当前子区域**、**降采样** 或 **进度条**（对齐 NFR-1.4）。
- 资源包建议 **按需懒加载**，首次进入渲染页显示加载进度。

##### 2.6.3.8 安全与回退
- 页面脚本与资源均来自 **应用内本地路径**；禁止执行远程任意脚本。
- **WebGL 初始化失败**、驱动黑名单或资源版本不匹配时：明确错误提示，并 **自动或手动回退 `legacy`**；设置项可提供「固定使用 Deepslate / legacy / 自动」。

##### 2.6.3.9 启动行为与「更新 Deepslate」
- **不会在每次启动时从 GitHub 下载 Deepslate 源码**：与 [vscode-nbt](https://github.com/misode/vscode-nbt) 类似，Deepslate 为 **npm 依赖，在构建阶段打入静态 JS**，随安装包分发。
- **可能联网的场景**（与「Deepslate 代码」区分）：**Minecraft 方块资源**（blockstates / models / 图集等）可在首次使用或版本切换时 **下载并缓存在本地**（如 `data/render_assets/`），不应在无操作时静默高频请求。
- **用户可控更新**：在 **选项** 中提供「启动时检查渲染组件更新」（默认关闭，待更新源定义后实现）与 **手动「检查渲染组件更新」**（见 **§2.0.4 FR-O.4 / FR-O.5**）。若将来提供独立于主程序的渲染资源包，通过清单 URL 比对版本并下载；**升级主程序**仍是获取新构建内嵌 Deepslate 修订的主要方式。

##### 2.6.3.10 NBT「完整入镜导出」参数化（透视/正交）
- 目标：在保持当前旋转（`cRot`）与当前画布分辨率的前提下，使结构尽量完整入镜；参数可在选项页调整并持久化。
- 宿主（Python）将设置项注入页面全局对象 `window.__lbaExportFullParams`；前端导出函数读取后参与距离估算。
- 若注入缺失或值非法，前端必须回退到内置默认值，保证导出行为稳定可预测。

参数定义（默认值）：

| 设置键 | 作用公式 | 默认值 |
|---|---|---|
| `nbt_export_full_margin` | `r = diag * 0.5 * margin`（共用入镜边距） | `1.22` |
| `nbt_export_full_perspective_min_distance` | 透视距离下限 `max(minDist, ...)` | `6.0` |
| `nbt_export_full_perspective_diag_extra` | 透视附加项 `+ diag * diagExtra` | `0.12` |
| `nbt_export_full_orthographic_need_half_padding` | 正交半高需求 `needHalfH = r + padding` | `1.0` |
| `nbt_export_full_orthographic_height_scale` | 正交换算 `cDist = needHalfH / scale + ...` 与 `orthoHalfHeight = cDist * scale` | `0.38` |
| `nbt_export_full_orthographic_diag_extra` | 正交附加项 `+ diag * diagExtra` | `0.15` |
| `nbt_export_full_orthographic_min_distance` | 正交距离下限 `max(minDist, ...)` | `12.0` |
| `nbt_export_full_orthographic_half_height_min` | 正交投影半高下限 `max(cDist * scale, halfHeightMin)` | `2.5` |

实现约束：
- 仅 NBT Viewer 的「导出…」使用该参数集；Deepslate 回退页可后续按同语义补齐。
- UI 中每个参数控件需通过提示文案展示“默认值”，便于用户按默认配置回退与调参对照。

#### 2.6.4 界面与字段
##### 2.6.4.1 UI壳

| 区块     | 功能项    | UI组 | 说明               | 优先级 |
| ------ | ------ | --- | ---------------- | --- |
| Line.1 | 【材料列表】 |     | 按钮，用于打开材料列表      |     |
| Line.1 | 渲染器    |     | 下拉框，目前仅提供"RSBIN" |     |
| Line.2 | 渲染器主界面 |     | 渲染器内部UI          |     |
##### 2.6.4.2 RSBIN渲染器

| 区块        | 功能项      | UI组  | 说明                              | 优先级 |
| --------- | -------- | ---- | ------------------------------- | --- |
| Line.1    | 构建模式     | 构建   | 下拉框，提供“普通模式”、“快速模式（实验性）”、“完整纹理” |     |
| Line.1    | 预生成分层    | 构建   | 复选框                             |     |
| Line.2    | 【开始构建】   | 构建   | 按钮                              |     |
| Line.2    | 【打开浏览弹窗】 | 构建   | 按钮                              |     |
| Line.3L   | 嵌入式查看器   | 渲染页  | 用于放置嵌入式viewer                   |     |
| Line.3R   | 控制面板     | 控制面板 | 用于放置控制项                         |     |
| Line.3R.1 | 【重置视角】   | 控制面板 | 按钮，用于重置视角                       |     |




---
### 2.7 模块七：方块替换（Replace）
#### 2.7.1 功能描述
方块替换模块用于将投影内特定方块批量替换为另一种方块。替换任务以**替换单元**为最小组织单位，一次执行可包含多个替换单元，按顺序依次执行。

#### 2.7.2 替换单元（Replace Unit）
**替换单元**是描述”将一组方块替换为另一组方块”操作的最小单位。它可以表达以下需求：
- 将单一方块替换为另一方块
- 将多种方块统一替换为同一方块
- 将方块替换为多个目标方块之一（依权重随机选取）

每个替换单元由两个部分组成：**输入端（Input）** 和 **输出端（Output）**。

##### 2.7.2.1 输入端
- 接受**一个或多个条目**
- 每个条目包含：**方块名称**（Block ID）以及可选的**方块状态**（Block State）
- 执行时，软件扫描投影内所有方块，找出符合输入端任一条目的方块作为替换源
- 不指定方块状态时，匹配该方块的所有状态变体

##### 2.7.2.2 输出端
- 接受**一个或多个条目**
- 每个条目包含：**方块名称**（Block ID）、可选的**方块状态**（Block State）以及**权重（Weight，正整数）**
- 执行时，每个命中位置的方块将被替换为某个输出端条目：
  - 若输出端仅有一个条目，则全部替换为该条目
  - 若输出端有多个条目，则按各条目权重比例随机选取；某条目选取概率 = `该条目权重 / 所有输出条目权重之和`

##### 2.7.2.3 方块选择方式
在输入端与输出端添加条目时，可通过以下两种方式选择方块：

| 入口 | 说明 | 适用场景 |
|------|------|------|
| **创造模式物品栏** | 使用软件内置的创造模式物品栏 UI 浏览并单选方块 | 添加单个具体方块条目 |
| **枚举器** | 通过枚举器模块按集合或集合表达式批量选择，一次可向端口添加多个条目 | 批量添加一类方块（如所有原木） |

##### 2.7.2.4 替换执行逻辑
1. 按卡片从上到下的顺序遍历所有替换单元
2. 对每个替换单元，扫描投影内全部方块位置，找出所有符合输入端的命中位置
3. 对每个命中位置，按输出端权重随机选取目标条目并执行替换
4. 替换结果写入内存中的投影方块数据；全部单元执行完成后，由用户决定是否保存到文件

#### 2.7.3 功能点
| 编号     | 功能点               | 描述                                                 | 优先级 |
| ------- | ----------------- | -------------------------------------------------- | --- |
| FR-P.1  | 模块入口              | 在导航中提供替换入口并接入活动文件                                  | P2  |
| FR-P.2  | 替换单元管理            | 支持添加、删除、上移/下移替换单元                                  | P1  |
| FR-P.3  | 条目输入——创造模式物品栏     | 通过创造模式物品栏选择单个方块，加入输入端或输出端                          | P1  |
| FR-P.4  | 条目输入——枚举器         | 通过枚举器批量选择方块集合，加入输入端或输出端；支持集合名与集合表达式               | P1  |
| FR-P.5  | 方块状态精确匹配          | 条目可指定方块状态；不指定时匹配该方块所有状态变体                          | P1  |
| FR-P.6  | 输出端多目标与权重         | 输出端支持多个条目，每个条目设权重，按权重随机选取目标方块                      | P1  |
| FR-P.7  | 扫描与评估             | 正式替换前先扫描投影，统计各替换单元命中的方块数量，供用户确认                    | P1  |
| FR-P.8  | 批量替换执行            | 顺序执行所有替换单元，写入内存；执行完成后由用户保存到文件                      | P1  |
| FR-P.9  | 预设管理              | 保存/加载当前所有替换单元为命名预设文件，支持删除预设                        | P2  |

#### 2.7.4 界面与字段
##### 2.7.4.1 主界面

| 区块     | 功能项          | UI组 | 说明                     | 优先级 |
| ------ | ------------ | --- | ---------------------- | --- |
| Line.1 | 预设组          |     | 下拉框，用于选择已保存的预设组        |     |
| Line.1 | 【保存预设】       |     | 保存当前所有替换单元为一个预设文件      |     |
| Line.1 | 【删除该预设】      |     | 删除选定的预设文件              |     |
| Line.1 | 【打开预设文件夹...】 |     | 打开预设文件夹                |     |
| Line.2 | 【添加替换单元】     |     | 添加一个空的替换单元卡片           |     |
| Line.2 | 【清空所有】       |     | 清空所有替换单元               |     |
| Line.3 | 替换单元区域       |     | 可滚动区域，按顺序放置替换单元卡片      |     |
| 页脚     | 【扫描并评估】      |     | 扫描投影，统计各替换单元命中方块数量，右对齐 |     |
| 页脚     | 【替换方块】       |     | 执行所有替换单元，右对齐           |     |

##### 2.7.4.2 替换单元卡片

每个替换单元以**卡片（Card）**形式呈现，卡片内部左右分为两列：左列为**输入端**，右列为**输出端**，两列之间居中显示**箭头（→）**以指示替换方向。

| 区块       | 功能项          | 说明                                                 | 优先级 |
| -------- | ------------ | -------------------------------------------------- | --- |
| 卡片头部     | 单元标题         | 显示该替换单元编号（如"替换单元 1"），支持折叠/展开                       | P1  |
| 卡片头部     | 【↑ 上移】【↓ 下移】 | 调整该替换单元在列表中的执行顺序                                   | P2  |
| 卡片头部     | 【删除单元】       | 删除该替换单元                                            | P1  |
| 输入端      | 条目列表         | 每行显示方块图标、方块名称（若方块有方块状态，则在名字后添加\*符号）；可上下拖拽排序        | P1  |
| 输入端      | 条目——删除按钮     | 每个条目行内提供【×】删除按钮                                    | P1  |
| 输入端      | 【添加条目...】    | 打开创造模式物品栏以选择物品或选择整个物品组                             | P1  |
| 输出端      | 条目列表         | 每行显示方块图标、方块名称（若方块有方块状态，则在名字后添加\*符号）以及权重输入框；可上下拖拽排序 | P1  |
| 输出端      | 条目——权重输入框    | 每个条目行右侧，整数输入，默认值为 1；权重为 0 时该条目不参与随机选取              | P1  |
| 输出端      | 条目——删除按钮     | 每个条目行内提供【×】删除按钮                                    | P1  |
| 输出端      | 【添加条目...】    | 打开创造模式物品栏以选择物品或选择整个物品组                             | P1  |
| 卡片底部（可选） | 命中数量提示       | 执行【扫描并评估】后，在卡片底部显示该单元命中的方块数量                       | P1  |





---
### 2.8 模块八：材料列表（独立窗口，优先）
#### 2.8.1 功能描述
展示投影文件在不同上下文（整体/层级/区域/外部文件）下的材料列表，采用缓存优先 + 后台异步刷新策略。
#### 2.8.2 界面与字段

| 区块     | 功能项   | 说明                     | 优先级 |
| ------ | ----- | ---------------------- | --- |
| Line.1 | 工作簿   | 用于决定当前表显示的材料来源（见2.8.4） | P0  |
| Line.2 | 功能按钮组 | （见2.8.5）               | P0  |
| Line.3 | 统计容器  | 复选框，用于切换统计容器开关         | P1  |
| Line.3 | 统计实体  | 复选框，用于切换统计实体开关         | P1  |
| Line.4 | 材料列表  |                        | P0  |

#### 2.8.3 缓存与调度
| 项 | 说明 | 优先级 |
|----|------|--------|
| 异步预加载 | 选定文件后立即后台加载材料列表 | P0 |
| 切换挂起 | 加载中切换文件时，旧任务挂起，新文件优先 | P0 |
| 缓存目录 | 缓存写入 `data/cache` | P0 |
| 缓存主键 | 使用完整文件路径作为主键 | P0 |
| 失效判定 | 文件在软件外部被改动则缓存失效 | P0 |
| 缓存读取策略 | 若存在缓存，优先读取缓存（即使已失效），并以灰色文本展示 | P0 |
| 刷新策略 | 缓存展示后立即后台异步重算，完成后替换为最新内容 | P0 |
#### 2.8.4 工作簿（Line.1）
“工作簿”下拉框位于独立窗口第一行，宽度占整行，用于切换材料列表语义视角。切换工作簿不会清空工作簿数据对象，仅切换当前展示与数据来源。

| 工作簿项 | 说明 | 优先级 |
|----------|------|--------|
| 整个投影 | 基于当前激活文件统计全部区域材料 | P0 |
| 选定层级 | 基于当前层级条件统计材料 | P1 |
| 选定区域 | 基于当前选中子区域统计材料 | P0 |
| 自定义文件 | 直接从 CSV 打开材料列表，不依赖当前投影统计流程 | P1 |
#### 2.8.5 功能按钮组（Line.2，靠左）
| 控件              | 行为                                          | 优先级 |
| --------------- | ------------------------------------------- | --- |
| 【重新加载】          | 将当前缓存标记为“已失效”，并重新从投影文件加载当前工作簿数据             | P0  |
| 【重新加载】（自定义文件模式） | 当工作簿为“自定义文件”时，该按钮替换为“浏览材料列表...”并用于选择 CSV 文件 | P0  |
| 【写入艺术表】          | 弹出保存对话框，导出当前表格                              | P0  |
| 【写入CSV】          | 弹出保存对话框，导出当前表格                              | P0  |
| 【倍数输入框】         | 支持上下调节，用于控制列表正文材料数量的倍数显示                    | P0  |

#### 2.8.6 列表正文
| 表头 | 说明 | 优先级 |
|------|------|--------|
| 图标 | 方块图标列。默认 2D；可切换 3D（首版非优先） | P0/P2 |
| 名称 | 默认显示标准译名；右键该行可切换表头语义为“方块ID” | P0 |
| 总计 | 显示统计总数量 | P0 |
图标显示扩展设置（非优先）：
- 设置项：`方块图标渲染模式`（`2D`/`3D`，默认 `2D`）
- 首次切换至 `3D` 时，从 Minecraft Wiki 下载所需图标资源
#### 2.8.7 悬浮浮窗（跟随鼠标）
当鼠标悬浮在列表项时，显示跟随鼠标的浮窗，包含：
- **项目**：同一行显示图标与译名
- **ID**：显示方块 ID
- **总计**：显示可拆解统计值，格式为  
  `总数 = 组数 x 64 + 余数 = 盒数 潜影盒`
计算规则：
- `组数 = 总数 / 64`（舍弃小数）
- `余数 = 总数 % 64`
- `盒数 = 总数 / 1728`（保留 2 位小数）
- 浮窗内变量数字使用加粗样式
相关设置项：
- `材料统计数字计算时机`：`预计算` 或 `即时演算`
  - `预计算`：在列表数据生成阶段先算好浮窗数字
  - `即时演算`：悬浮时按当前总数动态计算
---
### 2.9 模块九：多语言支持（i18n）
#### 2.9.1 功能描述
根据 JSON 语言文件切换界面显示文本。
#### 2.9.2 功能点
| 编号 | 功能点 | 描述 | 优先级 |
|------|--------|------|--------|
| FR-I18N.1 | 语言包加载 | 启动加载默认语言 JSON | P1 |
| FR-I18N.2 | 运行时切换 | 切换语言后刷新 UI 文本 | P1 |
| FR-I18N.3 | 回退机制 | 缺失翻译键时回退默认语言 | P1 |

语言资源目录约定（实现）：

- 内置语言源文件拆分为：`lang/zh_cn.json`（Minecraft 标准语言键）与 `lang/category.json`（分类关键词）。
- 首次运行时将内置语言复制到：`data/minecraft-assets/language/initial/`；后续运行时内置语言读取以该目录为准。
- 外部下载语言按来源分目录存储，`github/InventivetalentDev` 来源写入：`data/minecraft-assets/language/github/InventivetalentDev/<branch>/<lang>.json`，避免跨来源重复命名冲突。
---
### 2.10 模块十：枚举器（Enumerator）
#### 2.10.1 功能描述
枚举器用于列出 Minecraft 数据值，并提供可复制、可命名、可计算、可复用的数据值集合。它不是单纯的列表浏览器，而是面向替换、搜索、筛选、统计判定等模块的“集合工作台”。

本模块以某一 Minecraft 版本的数据值总表作为 **全集 `U`**。用户可从 `U` 中选择任意数量的数据值，生成命名集合（如 `A`、`N`、`Q`），并通过并集、交集、差集、补集、映射等操作得到新的集合或映射关系。集合结果可复制到剪贴板，也可在替换、搜索、筛选等页面粘贴或引用。

#### 2.10.2 数据值范围
| 类型      | 示例                                               | 用途             | 优先级 |
| ------- | ------------------------------------------------ | -------------- | --- |
| 方块 ID   | `minecraft:stone`、`minecraft:netherrack`         | 方块替换、材料筛选、投影判定 | P0  |
| 方块状态值   | `minecraft:oak_stairs[facing=north,half=bottom]` | 精确替换、精确筛选      | P1  |
| 物品 ID   | `minecraft:quartz`、`minecraft:redstone`          | 材料列表、物品筛选      | P1  |
| 标签 / 分类 | `minecraft:logs`、`minecraft:mineable/pickaxe`    | 批量选择、集合构造      | P1  |
| 自定义分类   | `<br>`下界岩制品N`、`石英制品Q                             | 用户知识库、投影材料判定   | P0  |

数据源可来自 `misode/mcmeta`、本地 `client.jar` 提取结果、项目内置资源或用户自定义导入；进入枚举器前应统一整理为版本化缓存，例如 `data/minecraft-assets/game-data/<version>/values.json`。

#### 2.10.2.1 基础全集来源（首批实现）
首批实现不直接从 `mcmeta` 或 `client.jar` 生成全集，而是先以 **Minecraft Wiki** 作为基础全集来源。目标是先拿到可稳定落盘、可直接做集合运算的 4 个基础集合文件。

统一规则：
- 以页面表格中表头为 **`Resource location`** 的列作为提取源。
- 提取结果为**字符串数组**，供并集、交集、差集、去重等集合运算直接使用。
- 同一来源页面若存在多个目标表，则按出现顺序提取后合并，再做去重。
- 这 4 个集合均视为**基础集合**，为枚举器提供最小可用全集能力。

| 集合名 | 来源页面 | 提取规则 | 本地文件 |
|------|------|------|------|
| 方块 | `https://minecraft.wiki/w/Java_Edition_data_values/Blocks` | 提取主表 `Resource location` 列 | `DV_Blocks.json` |
| 物品 | `https://minecraft.wiki/w/Java_Edition_data_values/Items` | 提取主表 `Resource location` 列 | `DV_Items.json` |
| 魔咒 | `https://minecraft.wiki/w/Java_Edition_data_values` | 提取主数据值页中魔咒相关表的 `Resource location` 列 | `DV_Enchantments.json` |
| 实体 | `https://minecraft.wiki/w/Java_Edition_data_values/Entities` | 提取页面内多个实体表的 `Resource location` 列，合并后去重 | `DV_Entities.json` |

#### 2.10.2.2 基础全集文件的生命周期约束
- 4 个基础集合文件保存在**同一个目录**下，作为枚举器运行时的基础数据输入。
- 这些文件在枚举器界面中**不可轻易删除**；枚举器内优先提供“刷新/重新抓取/重新应用”，不提供常规删除主路径。
- 若用户确实需要移除基础集合文件，可在“游戏资源管理”模块中执行整体删除。
- 当任一基础集合文件缺失时，枚举器应提示用户重新获取，而不是静默降级为一个空集合。

#### 2.10.3 功能点
| 编号      | 功能点   | 描述                                                        | 优先级 |
| ------- | ----- | --------------------------------------------------------- | --- |
| FR-E.1  | 数据值全集 | 按 Minecraft 版本加载数据值总表，形成全集 `U`                            | P0  |
| FR-E.2  | 浏览与搜索 | 支持按 ID、译名、类型、标签、来源分类筛选数据值                                 | P0  |
| FR-E.3  | 多选与复制 | 用户可选择一个或多个数据值，复制为纯 ID 列表、换行列表、JSON 数组或集合表达式               | P0  |
| FR-E.4  | 命名集合  | 将当前选择保存为命名集合，支持显示名与短符号，如 `下界岩制品N`                         | P0  |
| FR-E.5  | 手动分类  | 支持将数据值拖动或批量加入自定义集合，形成用户维护的分类库                             | P1  |
| FR-E.6  | 集合计算  | 支持用四则运算符表示 `∪/+` 并集、`∩/*` 交集、`-` 差集、补集、去重（对称差集）、包含关系与空集判断 | P0  |
| FR-E.7  | 集合映射  | 支持将集合中的多个源数据值映射到一个或多个目标数据值，用于替换等模块                        | P1  |
| FR-E.8  | 跨模块引用 | 替换、搜索、筛选、材料列表等模块可粘贴集合内容或引用已保存集合                           | P0  |
| FR-E.9  | 版本标识  | 集合需记录来源 Minecraft 版本；跨版本使用时提示缺失、改名或不兼容项                   | P1  |
| FR-E.10 | 持久化   | 命名集合与自定义分类保存到 `data/` 下，避免随安装包更新丢失                        | P0  |

#### 2.10.4 典型用例：批量替换集合
1. 枚举器加载某版本数据值全集 `U`。
2. 用户在 `U` 内选择任意数量的方块，保存或复制为集合 `A`。
3. 用户进入“替换（Replace）”页面，将集合 `A` 粘贴到源方块输入区。
4. 用户将目标方块设为 `minecraft:stone`。
5. 替换模块解释为：将 `A` 中全部方块批量替换为石头。

约束：
- 集合复制结果必须便于人工阅读与二次编辑。
- 替换模块粘贴集合时，应能识别列表、JSON 数组、集合名或集合表达式。
- 若集合中包含替换模块暂不支持的数据值类型，应在执行前列出并要求用户确认或剔除。

#### 2.10.5 典型用例：材料来源判定
用户可手动维护若干集合，例如：

- `下界岩制品|N`：包含下界岩及其制品方块。
- `石英制品|Q`：包含石英相关方块。
- `L`：某个投影文件的材料列表集合，可由材料列表窗口或统计模块生成。

若表达式满足：

```text
L ∩ (N ∪ Q) = ∅
```

则可判断该投影材料列表中不包含下界岩制品与石英制品。该判断可作为“材料是否均可在主世界获取”的一个条件；实际判定规则允许继续叠加更多集合与表达式，不应固定写死为单一条件。

#### 2.10.6 界面与字段
枚举器是一个子窗口，遵循设置内“子窗口行为”的值。
枚举器分为左右两列，左列为集合列表，右列为编辑页面
##### 集合列表

| 区块     | 功能项  | 说明                                                                                    | 优先级 |
| ------ | ---- | ------------------------------------------------------------------------------------- | --- |
| Line.1 | 搜索框  |                                                                                       |     |
| Line.1 | 分类   | 下拉框，目前分类分别为“所有分类”，“全集”（位于base/），“版本”（位于version/），“创造模式”（位于creative/），“自定义”（位于custom/） |     |
| Line.2 | 集合列表 | 复用表格样式，表头只有“名称”，表项可以拖拽到右侧编辑区                                                          |     |
##### 编辑区
分为左右两列，左列为来源列，右列为编辑列

| 区块        | 功能项     | 说明                                                                                      | 优先级 |
| --------- | ------- | --------------------------------------------------------------------------------------- | --- |
| Line.1L.1 | 来源集合    | 输入框，此处输入现有集合或其计算式（如“方块全集”-“红石方块”）                                                       |     |
| Line.1L.1 | 减去右侧    | 复选框，选中后左侧集合不再出现右侧集合的内容，相当于“左侧集合”-“右侧集合”                                                 |     |
| Line.1L.2 | 来源列数组列表 | 复用表格样式，表头为“图标”，“名称”，其中图标来源于目前正在使用的方块（和“材料列表”同源），物品图标；表项可以拖拽到右侧编辑列，表示向右侧添加一个表项；右键点击可快速添加 |     |
| Line.1R.1 | 编辑集合名称  | 输入框，此处可更改集合名称                                                                           |     |
| Line.1R.2 | 编辑列数组列表 | 复用表格样式，表头为“图标”，“名称”，其中图标来源于目前正在使用的方块（和“材料列表”同源），物品图标；表项可上下拖拽以排序，或拖向左侧以删除；右键点击可快速删除      |     |

##### 页脚
| 区块  | 功能项    | 说明                                  | 优先级 |
| --- | ------ | ----------------------------------- | --- |
| 右1  | 【新增集合】 | 新增一个“自定义”类型的集合，其他类型的集合需要手动移动文件到指定目录 |     |
| 右2  | 【保存副本】 |                                     |     |
| 右3  | 【保存集合】 |                                     |     |

#### 2.10.7 交互与表现
| 区块 | 说明 | 优先级 |
|------|------|--------|
| 数据值表 | 展示 ID、译名、类型、标签、来源版本、所属自定义集合 | P0 |
| 集合侧栏 | 展示已保存集合，支持新建、重命名、复制、删除、导入导出 | P0 |
| 表达式区 | 输入或组合 `A ∪ B`、`L ∩ (N ∪ Q)` 等表达式并预览结果 | P1 |
| 结果区 | 显示计算后的数据值列表、数量、差异项和复制按钮 | P0 |
| 映射区 | 支持源集合到目标值的映射关系编辑，供替换模块读取 | P1 |

#### 2.10.8 与其他模块的关系
- **替换（Replace）**：源方块输入支持粘贴枚举器集合；目标方块可从枚举器选择单值或映射结果。
- **材料列表**：材料列表可导出为集合 `L`，用于与用户集合做交集、差集和包含判断。
- **搜索 / 筛选**：列表筛选条件可引用枚举器集合，避免重复手动输入大量 ID。
- **游戏资源管理**：枚举器依赖版本化游戏数据缓存；当缓存缺失时应引导用户下载 `mcmeta` 资源或从本地 `client.jar` 导入。

---
### 2.11 模块十一：游戏资源管理

#### 2.11.2 界面与字段
##### 通用页脚

| 区块  | 功能项        | 说明                | 优先级 |
| --- | ---------- | ----------------- | --- |
| 左1  | 状态文本       | 显示当前操作状态          |     |
| 右1  | 【刷新】       | 按钮                |     |
| 右2  | 【健康检查】     | 按钮                |     |
| 右3  | 【打开根目录...】 | 按钮，用于打开装载这些内容的根目录 |     |
##### 方块图标

| 区块     | 功能项    | 说明                                                                                                                                                                                                                            | 优先级 |
| ------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| Line.1 | 标题与副标题 |                                                                                                                                                                                                                               |     |
| Line.2 | 来源     | 下拉框，目前默认为“内建”，还有“本地目录”，“CCVault”，“Minecraft Wiki”                                                                                                                                                                             |     |
| Line.3 | 子选项1   | 下拉框，目前无内容，占位                                                                                                                                                                                                                  |     |
| Line.3 | 子选项2   | 下拉框，目前无内容，占位                                                                                                                                                                                                                  |     |
| Line.4 | 【获取】   | 按钮，来源=内建：显示为【重新解包】；来源=本地目录：显示为【导入...】；来源=CCVault/MinecraftWiki：显示为【下载】                                                                                                                                                        |     |
| Line.5 | 内容表格   | 表头：名称：来源=内建的显示为“（内建）”，来源=本地目录的显示为“`目录名称`/”，来源=CCVault/MinecraftWiki的显示为“CCVault”或“Minecraft Wiki”；来源：按需显示为“builtin”，“local”，“vault”，“wiki”；版本：目前全部显示为“未知”，等待枚举器完成后再写判断逻辑；用于：按需显示为“材料”，“分层”，“材料/分层”；操作：显示按钮：【用于材料】【用于分层】【删除登记】 |     |
##### 物品图标

| 区块     | 功能项    | 说明                                                                                                                                                                                                        | 优先级 |
| ------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| Line.1 | 标题与副标题 |                                                                                                                                                                                                           |     |
| Line.2 | 来源     | 下拉框，目前默认为“内建”，还有“本地目录”，“CCVault”，“Minecraft Wiki”                                                                                                                                                         |     |
| Line.3 | 子选项1   | 下拉框，目前无内容，占位                                                                                                                                                                                              |     |
| Line.3 | 子选项2   | 下拉框，目前无内容，占位                                                                                                                                                                                              |     |
| Line.4 | 【获取】   | 按钮，来源=内建：显示为【重新解包】；来源=本地目录：显示为【导入...】；来源=CCVault、MinecraftWiki：显示为【下载】                                                                                                                                    |     |
| Line.5 | 内容表格   | 表头：名称：来源=内建的显示为“（内建）”，来源=本地目录的显示为“`目录名称`/”，来源=CCVault/MinecraftWiki的显示为“CCVault”或“Minecraft Wiki”；来源：按需显示为“builtin”，“local”，“vault”，“wiki”；版本：目前全部显示为“未知”，等待枚举器完成后再写判断逻辑；状态：按需显示为“已激活”；操作：显示按钮：【应用】【删除登记】 |     |
##### 语言
| 区块     | 功能项    | 说明                                                                                                                                                                                                                            | 优先级 |
| ------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| Line.1 | 标题与副标题 |                                                                                                                                                                                                                               |     |
| Line.2 | 来源     | 下拉框，目前默认为“内建”，还有“本地JSON”，“Github/InventivetalentDev”                                                                                                                                                                          |     |
| Line.3 | 子选项1   | 下拉框，来源=Github/InventivetalentDev：作为版本下拉框                                                                                                                                                                                      |     |
| Line.3 | 子选项2   | 下拉框，来源=Github/InventivetalentDev：作为语言下拉框                                                                                                                                                                                      |     |
| Line.4 | 【获取】   | 按钮，来源=内建：显示为【重新解包】；来源=本地JSON：显示为【导入...】；来源=Github/InventivetalentDev：显示为【下载】                                                                                                                                                  |     |
| Line.4 | 内容表格   | 表头：名称：来源=内建的显示为“（内建）zh_cn”，来源=本地JSON的显示为“`文件名称（含扩展名）`”，来源=Github/InventivetalentDev的显示为“`语言代号`”；来源：按需显示为“builtin”，“local”，“github”；语言：按需显示语言代号；版本：从github来的按需显示，内置与本地文件显示“未知”，等待枚举器完成后再写判断逻辑；状态：按需显示为“已激活”；操作：显示按钮：【应用】【删除登记】 |     |
##### 游戏数据
| 区块     | 功能项    | 说明                                                                                                                                           | 优先级 |
| ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| Line.1 | 标题与副标题 |                                                                                                                                              |     |
| Line.2 | 来源     | 下拉框，目前默认为“内建”，还有“本地JSON”                                                                                                                     |     |
| Line.3 | 子选项1   | 下拉框，目前无内容，占位                                                                                                                                 |     |
| Line.3 | 子选项2   | 下拉框，目前无内容，占位                                                                                                                                 |     |
| Line.4 | 【获取】   | 按钮，来源=内建：显示为【重新解包】；来源=本地JSON：显示为【导入...】；                                                                                                     |     |
| Line.4 | 内容表格   | 表头：名称：来源=内建的显示为“（内建）”，来源=本地JSON的显示为“`文件名称（含扩展名）`”；来源：按需显示为“builtin”，“local”；版本：内置与本地文件显示“未知”，等待枚举器完成后再写判断逻辑；状态：按需显示为“已激活”；操作：显示按钮：【应用】【删除登记】 |     |
##### 枚举全集
| 区块     | 功能项    | 说明                                                                                                                                                                                           | 优先级 |
| ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| Line.1 | 标题与副标题 |                                                                                                                                                                                              |     |
| Line.2 | 来源     | 下拉框，目前默认为“内建”，还有“Minecraft Wiki”                                                                                                                                                             |     |
| Line.3 | 子选项1   | 下拉框，目前无内容，占位                                                                                                                                                                                 |     |
| Line.3 | 子选项2   | 下拉框，目前无内容，占位                                                                                                                                                                                 |     |
| Line.4 | 【获取】   | 按钮，来源=内建：显示为【重新解包】；来源=MinecraftWiki：显示为【下载】                                                                                                                                                  |     |
| Line.4 | 内容表格   | 表头：名称：来源=内建的显示为“（内建）”，来源=本地JSON的显示为“`文件名称（含扩展名）`”；来源=MinecraftWiki的显示为“Minecraft Wiki”；来源：按需显示为“builtin”，“wiki”；日期：内置的显示为“2025/4/22”，来源=MinecraftWiki显示获取日期；状态：按需显示为“已激活”；操作：显示按钮：【应用】【删除登记】 |     |


---
## 3. 非功能性需求
### 3.1 性能需求
| 编号 | 需求 | 指标 |
|------|------|------|
| NFR-1.1 | 启动时间 | 冷启动建议不超过 5 秒 |
| NFR-1.2 | 预览图加载 | 预览图解析必须异步，不阻塞主线程 |
| NFR-1.3 | 缓存响应 | 有缓存时应优先秒级可见 |
| NFR-1.4 | 大文件处理 | 统计/渲染需具备后台进度反馈 |
### 3.2 兼容性需求
| 编号 | 需求 | 说明 |
|------|------|------|
| NFR-2.1 | 操作系统 | 首版优先 Windows 10/11 |
| NFR-2.2 | 文件格式 | 支持 `.litematic`，重点兼容 version=5 |
| NFR-2.3 | 编码支持 | 支持 UTF-8、中文路径与中文元数据 |
### 3.3 可靠性需求
| 编号 | 需求 | 说明 |
|------|------|------|
| NFR-3.1 | 切换保护 | 切换活动文件前必须处理未保存确认 |
| NFR-3.2 | 缓存容错 | 缓存损坏或缺失不影响主流程 |
| NFR-3.3 | 可携目录 | 持久化目录位于程序同级 `data/` |
### 3.4 可用性需求
| 编号 | 需求 | 说明 |
|------|------|------|
| NFR-4.1 | 术语一致 | 明确区分“预览图”与“缩略图” |
| NFR-4.2 | 操作一致 | 全模块共享“活动文件”语义 |
| NFR-4.3 | 多语言 | UI 支持基于 JSON 的语言切换 |

---
## 4. 数据模型（草案）
### 4.1 核心实体关系
```
┌──────────────────────┐
│ SchematicFile        │
├──────────────────────┤
│ id                   │
│ full_path (unique)   │
│ file_name            │
│ file_version         │
│ mc_data_version      │
│ author               │
│ description          │
└──────────────────────┘
          │
          ├──────────────┐
          ▼              ▼
┌──────────────────┐   ┌──────────────────────┐
│ MaterialCache    │   │ RenderCache          │
├──────────────────┤   ├──────────────────────┤
│ file_path(pk)    │   │ file_path(pk)        │
│ region_key       │   │ region_key           │
│ payload_json     │   │ thumbnail_path       │
│ cache_mtime      │   │ preview_written      │
└──────────────────┘   └──────────────────────┘
```
### 4.2 关键字段
| 字段 | 含义 |
|------|------|
| `full_path` | 文件完整路径，作为缓存索引基础 |
| `cache_mtime` | 缓存生成时的源文件时间戳 |
| `payload_json` | 材料统计/图表等可复用计算结果 |

### 4.3 枚举器数据模型
#### 4.3.1 版本化游戏数据缓存
| 字段 | 含义 |
|------|------|
| `version` | Minecraft 版本号或数据版本标识 |
| `source` | 数据来源，如 `minecraft_wiki`、`mcmeta`、`client_jar`、`builtin`、`custom` |
| `values` | 数据值列表，通常为可直接参与集合运算的字符串数组；扩展结构可追加 ID、类型、译名、标签、状态属性等 |
| `generated_at` | 缓存生成时间 |

建议路径：`data/minecraft-assets/game-data/<version>/values.json`。

#### 4.3.1.1 基础全集文件（首批实现）
首批实现先落盘 4 个由 Minecraft Wiki 抽取得到的基础集合文件，统一放在同一目录下：`data/enumerator/base/`。

| 文件名 | 集合含义 | 来源 | 结构 |
|------|------|------|------|
| `DV_Blocks.json` | 方块基础集合 | `Java_Edition_data_values/Blocks` | JSON 字符串数组 |
| `DV_Items.json` | 物品基础集合 | `Java_Edition_data_values/Items` | JSON 字符串数组 |
| `DV_Enchantments.json` | 魔咒基础集合 | `Java_Edition_data_values` 主页中的魔咒表 | JSON 字符串数组 |
| `DV_Entities.json` | 实体基础集合 | `Java_Edition_data_values/Entities` 多表合并 | JSON 字符串数组 |

约束：
- 4 个文件属于枚举器基础输入，不作为普通用户集合写入 `sets.json`。
- 枚举器默认只消费这些基础文件与用户命名集合，不在界面中提供基础文件的常规删除入口。
- “游戏资源管理”模块会将抓取好的全集写入一行项目，可以对这些基础文件执行打包删除、重抓取或重建。

#### 4.3.2 命名集合
| 字段 | 含义 |
|------|------|
| `id` | 内部唯一 ID |
| `name` | 显示名，如 `下界岩制品` |
| `symbol` | 短符号，如 `N`、`Q`、`A` |
| `version` | 适用 Minecraft 版本 |
| `value_type` | 集合主要类型，如 `block`、`item`、`mixed` |
| `values` | 数据值 ID 列表 |
| `description` | 用户备注 |
| `updated_at` | 最近修改时间 |

建议路径：`data/enumerator/sets.json`；导入导出可使用同结构 JSON。

#### 4.3.3 表达式与映射
| 字段              | 含义                                                         |
| --------------- | ---------------------------------------------------------- |
| `expression`    | 集合表达式，如 `L ∩ (N ∪ Q)`                                      |
| `result_values` | 表达式计算后的数据值列表，可缓存也可即时计算                                     |
| `mapping`       | 替换映射，如 `{ "sources": ["A"], "target": "minecraft:stone" }` |

### 4.4 游戏资源管理与消费模型
#### 4.4.1 旧项目资源管理模型（已验证）
旧项目采用“**安装索引 + 激活槽位 + 运行时解析**”模式：资源先被下载/导入到用户数据目录，再写入 `installed.json`，业务模块不直接约定固定文件，而是先解析“当前生效资源”。

| 资源类型 | 安装位置 | 索引/状态 | 消费槽位 |
|------|------|------|------|
| 游戏语言 | `minecraft-assets/language/...` | `language/installed.json` + `active` | 全局单活跃语言 |
| 方块图标 | 2D图标`minecraft-assets/block_2d/...`、3D图标`minecraft-assets/block_icon/...` | `block_2d/installed.json`、`block_icon/installed.json` | `active_material_list`、`active_layering` |
| 物品图标 | `minecraft-assets/item/...` | `item/installed.json` | `active_layering` |

旧项目消费特征：
- 页面消费前先经资源解析层决定当前根目录或当前语言映射，而不是直接拼固定路径。
- 图标缓存与预热缓存绑定当前资源环境标签；当 `installed.json` 或激活项变化时自动失效。
- 同一种资源允许按业务场景拆分槽位，例如材料列表与分层可使用不同方块图标来源。

##### 4.4.1.1 旧项目语言资源的具体实现
| 项 | 旧项目实现 |
|------|------|
| 内建回退源 | 首次运行时将 `pack-in/lang/zh_cn.json` 解包到 `minecraft-assets/language/initial/zh_cn.json`；同时将 `pack-in/legacy/category.json` 解包到 `legacy/category.json` 供旧统计分类使用。 |
| 在线来源 | `https://github.com/InventivetalentDev/minecraft-assets` |
| 分支索引获取 | 先调用 GitHub API `repos/.../branches?per_page=100&page=N`；若匿名额度受限或失败，则回退 `git ls-remote --heads`；再失败则抓取 GitHub branches HTML 页面。 |
| 分支过滤 | 仅接受类似 `1.20`、`1.20.1`、`26.1` 这类版本分支名，并按数值版本倒序排序。 |
| 语言索引获取 | 先尝试读取 `raw.githubusercontent.com/.../assets/minecraft/lang/_list.json`；若无则退回 GitHub Contents API `contents/assets/minecraft/lang?ref=<branch>`；再失败则抓取该目录 HTML 页面并提取 `*.json`。 |
| 语言过滤 | 仅保留合法语言码，如 `zh_cn`、`en_us`、`zh_hans_cn`；下载后只保留 `block.`、`item.`、`entity.`、`effect.`、`enchantment.` 前缀键。 |
| 本地落盘 | 下载结果写入 `minecraft-assets/language/github/InventivetalentDev/<branch>/<lang>.json`。 |
| 索引写入 | 写入 `language/installed.json`，字段至少包含 `id`、`language`、`branch`、`source`、`file_relpath`、`active`、`installed_at`。 |
| 运行时读取 | `load_runtime_language_map()` 先读取 `installed.json` 中 `active=true` 的条目；若无可用项则回退 `initial/zh_cn.json`。 |

##### 4.4.1.2 旧项目方块图标资源的具体实现
| 项 | 旧项目实现 |
|------|------|
| 内建回退源 | 首次运行时将 `pack-in/arr-private/block.zip` 解压到 `minecraft-assets/block_2d/initial`；通过哨兵文件 `.lba_block_2d_seed` 与版本号避免重复解压。 |
| 在线来源 | `https://ccvaults.com/` |
| 访问令牌获取 | 先访问站点首页建立 Cookie，再向 `/api/token` 发送 `POST`，携带 `x-api-key`、`Origin`、`Referer`、`X-Requested-With` 等头，取得 Bearer token。 |
| 方块索引获取 | 优先调用 `/api/assets/20.%20Blocks`，遍历 `subcategories[].files[]` 生成 `(block_id, file_url)`；若为空，则回退 `/api/assets/all`，过滤 `category == "20. Blocks"` 的文件。 |
| 索引去重 | 以 `block_id = Path(file_name).stem.lower()` 去重，避免同名方块重复登记。 |
| 下载与处理 | 并行下载 PNG，若尺寸不是 `32x32` 则统一缩放到 `32x32`，最终保存到 `minecraft-assets/block_icon/vault/<block_id>.png`。 |
| 槽位登记 | 下载后调用 `register_vault_block_icon_slot()`，在 `block_icon/installed.json` 中登记一个 `vault:ccvaults` 条目，并默认将其设为 `active_material_list=true`、`active_layering=false`。 |
| 索引迁移 | 兼容旧版根目录 `block_visuals_installed.json`，启动时会拆分迁移到 `block_2d/installed.json` 与 `block_icon/installed.json`。 |
| 运行时解析 | 材料列表先根据 `active_material_list` 解析当前图标根目录；若无激活项则回退 `block_2d/initial`。路径解析优先尝试直连目录，再尝试 `assets/minecraft/textures/block/`、`textures/block/`、`block/`，最后 `rglob` 搜索。 |
| 缓存失效 | 图标解析缓存和预热缓存绑定当前图标根目录、目录时间戳及 `installed.json` 指纹；切换资源后自动失效。 |

#### 4.4.2 Nova 当前资源消费模型（现状）
Nova 当前采用“**工作区固定资源 + 启动后前端缓存**”模式：页面直接通过 service 层读取工作区内随应用分发的资源文件，运行时默认不存在“切换当前资源包”的状态层。

| 资源类型 | 当前来源 | 当前消费方式 | 当前限制 |
|------|------|------|------|
| 基础中文翻译 | `pack-in/lang/zh_cn.json` | 启动时加载到前端 `langMap` | 无激活语言概念 |
| BlockState 数据库 | `data/minecraft_blockstates/<version>.json` 与 `.zh_cn.json` | 启动时加载到前端 cache | 版本基本写死在代码常量 |
| 方块图标 | `block/*.png`、`item/*.png`、`pack-in/*.png` | 运行时按 block id 直接解析为 data URL | 无资源索引、无槽位切换 |

Nova 当前消费特征：
- 页面消费的是固定路径约定，不依赖用户态安装索引。
- 设置页当前更偏“路径存在性与健康检查”，而不是资源安装/切换管理器。
- 图标与语言缓存目前只按会话初始化，不按“资源版本/激活状态”自动失效。

#### 4.4.3 目标原则
若要把旧项目的资源管理能力迁入 Nova，应采用“**资源管理状态可切换，业务消费入口保持稳定**”原则：
- 页面仍只调用统一 service，如 `translateBlockId()`、`getBlockIconDataUrl()`。
- 是否使用内建资源、用户导入资源、在线下载资源，均由资源解析层和激活状态层决定。
- 允许保留 Nova 当前固定资源作为默认回退，以便离线冷启动始终可用。


---
## 5. 系统架构（草案）
### 5.1 分层结构
| 层级 | 说明 |
|------|------|
| UI 层 | 导航、表单、渲染视图、异步状态展示 |
| 业务层 | 激活文件上下文、SNBT 读写、统计与渲染调度 |
| 数据层 | 文件系统访问、设置管理、缓存管理 |
| 适配层 | **Deepslate WebView 桥接**（路径/二进制/JSON 注入、截图回传）与 **legacy** 渲染适配 |
### 5.2 关键约束
1. 文件解析、统计与渲染必须异步化，避免阻塞 UI。  
2. “属性（Properties）”是活动文件管理主入口。  
3. 缓存采用“先展示后刷新”策略，保证反馈速度。

### 5.3 旧项目资源模型迁入 Nova 的分层映射
| 目标层 | Nova 需要补的层/能力 | 说明 |
|------|------|------|
| UI 层 | 资源管理入口与管理对话框/页面 | 在设置页新增“游戏资源管理”入口，至少覆盖语言、方块图标、物品图标、版本化游戏数据四类资源；支持下载/导入/删除/应用/回退内建。 |
| UI 层 | 当前生效资源可视化 | 在设置页明确显示“当前语言”“材料列表图标来源”“分层图标来源”“当前数据版本”，避免用户只看到文件存在性而看不到实际消费状态。 |
| 业务层 | Resource Registry Service | 抽象出统一资源注册表读写服务，负责读取/写入 `installed.json`、维护资源元数据、暴露当前 active 状态。 |
| 业务层 | Resource Resolver | 将当前固定的 `i18n.ts`、`blockIconResolver.ts`、`blockstateDb.ts` 提升为“先解析激活资源、再返回消费结果”的入口，页面 API 尽量保持不变。 |
| 业务层 | 多槽位激活策略 | 至少支持：全局语言单活跃、方块图标按 `material_list` / `flake` 双槽位、物品图标按 `flake` 槽位；后续如 Render/Enumerator 有独立需求可继续扩展。 |
| 业务层 | 资源切换后的缓存失效 | 资源切换后需主动清空前端内存缓存、图标 Promise cache、预览缓存，并通知已打开页面重读；不能依赖用户重启应用。 |
| 数据层 | 用户态资源目录 | 在用户配置目录下建立稳定目录，例如 `minecraft-assets/language/`、`block_2d/`、`block_icon/`、`item/`、`game-data/`，与工作区只读资源解耦。 |
| 数据层 | 安装索引与迁移 | 定义 `installed.json` 结构、版本号、迁移规则与内建回退策略；旧索引字段变更时要可升级。 |
| 数据层 | 下载/导入/删除管线 | 提供下载、文件导入、去重、覆盖确认、删除、回滚与健康检查；避免 UI 直接操作路径。 |
| 适配层 | Tauri 文件/网络命令 | 当前前端主要读取工作区文件；迁移后需要补用户配置目录读写、下载、目录枚举、图片读取与路径健康检查命令。 |
| 适配层 | 前端事件广播 | 需要新增诸如 `resource-language-changed`、`resource-block-icons-changed`、`resource-game-data-changed` 事件，驱动页面热更新。 |

#### 5.3.1 对现有 Nova service 的直接映射
| 现有入口 | 当前模式 | 迁移后目标 |
|------|------|------|
| `initI18n()` / `translateBlockId()` | 固定读取 `pack-in/lang/zh_cn.json` | 改为读取“当前激活语言包”，无激活时回退内建。 |
| `loadDatabases()` / `translateKey()` / `translateValue()` | 固定读取 `data/minecraft_blockstates/26.1*.json` | 改为按当前游戏数据版本解析用户态或内建数据源；设置页可切换或下载版本。 |
| `getBlockIconDataUrl()` | 固定按 `block/`、`item/`、`pack-in/` 查找 PNG | 改为先解析当前槽位对应的图标根目录，再按统一候选策略查找；切换后缓存失效。 |
| `SettingsPage` 检查资源文件存在性 | 被动检查 | 升级为可操作的资源管理页，同时保留健康检查。 |

#### 5.3.2 实施顺序建议
1. 先补**数据层索引 + 适配层命令**，让 Nova 具备“安装资源、读取 active 状态”的基础能力。  
2. 再改**业务层 resolver**，保持页面调用接口尽量不变。  
3. 最后补**设置页资源管理 UI 与事件广播**，接通热切换与缓存失效。
---
## 6. 项目里程碑（建议）
| 阶段 | 目标 | 交付内容 |
|------|------|----------|
| Phase 1 | 核心闭环 | 投影库 + 属性读写 + 活动文件联动 |
| Phase 2 | 分析能力 | 统计分析 + 材料列表缓存 |
| Phase 3 | 可视化能力 | 分层与区域渲染（**Render 以 Deepslate 为主**）+ 方向控制 + 缩略图导出；`legacy` 并行改进 |
| Phase 4 | 增强功能 | 方块替换 + 枚举器集合输入 + i18n 完善 + 渲染方案扩展 |
| Phase 5 | 数据值工作台 | 枚举器全集、命名集合、集合表达式、映射与跨模块复用 |
---
## 7. 风险与应对
| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 大文件解析耗时 | 中 | 高 | 异步解析 + 缓存 + 进度提示 |
| 多渲染方案行为不一致 | 中 | 中 | 统一渲染接口（数据源一致），Deepslate / legacy 分别适配；导出分辨率策略写死或配置化 |
| WebView / WebGL 环境差异 | 中 | 中 | 检测 GPU 与 WebGL；失败时回退 `legacy` 并记录日志 |
| 渲染资源与数据版本错配 | 中 | 中 | 按 `mc_data_version` 选用资源包；缺失时提示下载/切换版本 |
| 缓存过期导致误读 | 中 | 中 | 时间戳校验 + 灰色缓存标识 + 后台刷新 |
| 版本兼容问题（非 v5） | 中 | 中 | 解析层按版本分支并保留降级提示 |
| 枚举器集合跨版本失效 | 中 | 中 | 集合记录来源版本；跨版本使用时标出缺失项、改名项与不兼容项 |
---
## 8. 附录
### 8.1 术语表
| 术语 | 说明 |
|------|------|
| 活动文件 | 当前被激活、供各模块读取/写入的 `.litematic` 文件 |
| 预览图 | 文件内 `PreviewImageData` 数组所表示图像 |
| 缩略图 | 软件渲染输出的 PNG 图像（渲染图） |
| 子区域 | 投影文件中可单独选择渲染/统计的区域单元 |
| 数据值 | Minecraft 中可被枚举、复制、筛选或替换引用的资源 ID 或状态值，如方块 ID、物品 ID、方块状态、标签等 |
| 全集 `U` | 当前 Minecraft 版本下枚举器加载的全部数据值集合 |
| 命名集合 | 用户从全集或表达式结果中保存的一组数据值，带显示名和短符号，如 `下界岩制品|N` |
| 集合表达式 | 对命名集合或临时集合进行并集、交集、差集、补集等计算的表达式 |
### 8.2 参考资源
- [Deepslate（npm）](https://www.npmjs.com/package/deepslate)
- [EndingCredits/litematic-viewer](https://github.com/EndingCredits/litematic-viewer)
- [misode/vscode-nbt](https://github.com/misode/vscode-nbt)（WebView + Deepslate + `litematicToStructure` 集成参考）
---
