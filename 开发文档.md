# Litematica-BA 开发文档

## 1. 文档定位

这份文档面向当前仓库的继续开发者，目标不是写成理想化设计稿，而是按 **2026-04-22 当前仓库实际进度** 说明：

- 这个项目现在已经做到了什么
- 代码主入口和模块边界在哪里
- 三种显示模式各自走哪条链
- Full Mode V2 当前已经收了哪些块族
- 最近改动较多、容易踩坑的地方有哪些
- 继续开发时，哪些层该动，哪些层尽量不要动

如果你是第一次接手这个仓库，建议先读这份文档，再读：

- `README.md`
- `docs/design.md`
- `投影格式文档.md`

---

## 2. 项目当前状态概览

### 2.1 项目是什么

`Litematica-BA` 是一个围绕 Minecraft `*.litematic` 投影文件的本地工具，目标是：

- 不开游戏也能读投影
- 查看 metadata / 区域 / 统计 / 分层
- 生成 3D 预览
- 最终把渲染链做成尽可能接近原版方块语义的离线 viewer

### 2.2 当前已形成的主架构

仓库现在不是单一脚本，而是“两套实现并存”的阶段：

1. 旧 Python/UI 侧
   - 位于 `script/`
   - 负责早期 UI、属性页、统计、部分工具功能

2. 新 Rust viewer-core + native viewer 链
   - 位于 `tools/viewer-core/src/`
   - 负责高性能解析、场景索引、三模式渲染、Full Mode V2

目前开发重心已经明显转向 Rust viewer-core 这一侧，尤其是：

- `mesh.rs`
- `full_mode_v2.rs`
- `native_viewer.rs`
- `nbt.rs`

### 2.3 当前进度的真实结论

现在仓库已经不是“只能读 metadata”的阶段，而是：

- `.litematic` 解析链完整可用
- scene/catalog 已经可驱动三种显示模式
- normal / fast / full 三模式入口已经接好
- Full Mode V2 已经形成独立运行链
- 但 V2 仍处在“按块族逐步收口 fidelity”的阶段

换句话说：

- 基础框架已经成立
- 但 Full Mode V2 还不是“全世界都修完了”

---

## 3. 当前仓库目录怎么读

根目录主要分成下面几类：

### 3.1 用户侧/资源侧目录

- `data/`
  - 项目运行数据
  - 包含 projection-library、backups 等
- `block/`
- `item/`
- `pack-in/`
- `lang/`
- `logo/`

### 3.2 旧 Python 逻辑

- `script/`

这一侧还保留：

- 属性读写
- 统计逻辑
- 一些历史工具功能

### 3.3 新核心实现

- `tools/viewer-core/src/`

这是当前最重要的开发目录。

### 3.4 UI / 桌面壳

- `desktop-ui/`
- `src/`
- `bin/`

其中：

- `bin/viewer-backend/litematica_native_viewer.exe`
  是当前桌面侧实际调用的 native viewer 二进制

### 3.5 文档与参考

- `docs/`
- `external_refs/`

`external_refs/` 里保留过参考项目和资源，用于比照实现。

### 3.6 根目录里的 Full Mode 验证文件

根目录会保留少量固定 fixture：

- `_full_mode_*fixture.litematic`

这些是迭代 Full Mode V2 时使用的最小样本，继续开发时优先复用 fixture，不要一上来盲改大场景。运行中生成的临时预览图、日志和 face map 属于本机验证产物，不作为固定资产提交。

---

## 4. Rust 核心模块说明

`tools/viewer-core/src/` 当前主要模块如下：

### 4.1 `nbt.rs`

作用：

- 负责读取 `.litematic`
- 定义 `LitematicRoot`、`MetadataNbt`、`RegionNbt` 等结构
- 处理 `BlockStates` 压缩 long array 的 palette 解码

这个模块负责“把文件还原成结构化数据”。

### 4.2 `mesh.rs`

作用：

- 从 catalog / region palette semantics 构建普通模式与快速模式需要的 mesh
- 做 palette render info、邻接裁面、occlusion 相关逻辑
- 当前也承担了 culling registry 的职责

最近比较重要的改动点：

- `BlockProperties`
- `CullingPolicy`
- `preserve_neighbor_faces`
- glass family 的邻接裁面特例

如果后面继续改“邻接面保留 / 同类例外 / whitelist”，优先在这里做。

### 4.3 `full_mode.rs`

作用：

- 定义 Full Mode 相关共享结构
- 包括：
  - `FullModeMaterialCache`
  - `FullModePaletteMaterial`
  - `FullModeModelQuad`
  - 相关验证函数

它更像 Full Mode 的公共数据定义层。

### 4.4 `full_mode_v2.rs`

这是当前最重要、最复杂的文件。

作用：

- Full Mode V2 的运行时 builder
- blockstate/model/material/template 主链
- typed blockstate / typed model 解析
- special family 的局部处理
- block overrides
- texture alias / atlas / material 生成

可以把它理解成：

**当前完整模式真实渲染语义的主战场**

### 4.5 `native_viewer.rs`

作用：

- native 窗口
- GPU 顶点/索引上传
- atlas 采样
- preview 输出

如果画面“全灰”、“蓝粉渐变”、“采样错位”，通常要查它；但最近已经把主要问题收进 V2 链里，当前阶段不要轻易大改这里。

### 4.6 `analyze.rs`

作用：

- 基于 `.litematic` 做统计分析

### 4.7 `visual.rs`

作用：

- metadata 输出等更偏展示层的构造

### 4.8 `model.rs`

作用：

- 一些输出结构和中间模型定义

---

## 5. 当前三种显示模式

项目当前有三模式：

- `normal`
- `fast_experimental`
- `full`

### 5.1 normal

特点：

- 稳
- 链路简单
- 更接近旧模式

适合：

- 基础预览
- 回归验证“是不是彻底炸了”

### 5.2 fast_experimental

特点：

- 更 aggressive 的裁面 / 合并 / 快路径
- 面向性能

风险：

- 不能拿它的行为代表 Full Mode V2

### 5.3 full

特点：

- 当前主战场
- 以 fidelity-first 为目标
- 已经分成旧 full 和新 Full Mode V2 语义，但运行路径现在主要围绕 V2 在推进

---

## 6. Full Mode V2 当前真实状态

### 6.1 目标

Full Mode V2 的目标是：

- state-aware
- blockstate -> model -> quads
- per-face material / UV / atlas
- 尽量接近原版 block model 语义
- 特殊块族允许独立 special family

### 6.2 当前已经成型的部分

已经成型的基础能力包括：

- 运行时 material atlas
- per-face material slot
- typed blockstate / typed model 主链
- parent flatten
- owned resolved model / element / face
- block overrides 入口
- popup / embedded 共享 native viewer 渲染路径

### 6.3 当前已经重点收过的块族

按最近进度，至少已经实做并反复调过：

- glass full block family
- rail family
- repeater
- hopper
- redstone_wire
- chest family
- barrel
- piston / sticky_piston / piston_head（正在继续收静态家族细节）

### 6.4 当前仍然不应该被“假设已完全完成”的部分

- moving_piston
- 各种仍未给真实 fixture 的特殊块
- 某些 tile entity / block entity 深度语义
- 仍在持续微调的 UV / local orientation 细节

---

## 7. Full Mode V2 的主数据流

当前 V2 可以概括成下面这条链：

1. 读取 `.litematic`
2. 得到 scene palette + property_pool
3. 对每个唯一 state 生成 `state_key`
4. 调 `build_state_template(...)`
5. 在这里分流：
   - water
   - moving_piston
   - redstone_wire
   - glass full block
   - repeater / barrel / rail / hopper / piston family
   - chest / cauldron / composter / sign 等
6. 生成 `TemplateQuad`
7. 转成 `FullModeModelQuad`
8. 生成 material atlas
9. native viewer 渲染

关键点是：

- 现在真正的“每个块怎么画”，都集中在 `build_state_template(...)` 和它继续分流出去的函数里

---

## 8. typed 主链现在是什么状态

### 8.1 typed blockstate / model

当前已经有一条 typed 主链，主要包含：

- typed blockstate 解析
- typed model 解析
- parent flatten
- `TypedResolvedModel`
- `ResolvedElement`
- `ResolvedFace`

### 8.2 这条链已经接进去的块

按当前代码，`build_state_template(...)` 里已经显式让这些块优先走 `typed_model_template(...)`：

- `redstone_wire`
- full glass blocks
- `repeater`
- `barrel`
- rail family
- `hopper`
- `piston`
- `sticky_piston`
- `piston_head`

### 8.3 当前不要轻易重构它

这条链已经牵涉：

- rail
- repeater
- hopper
- barrel
- piston family

如果你去大改：

- shared rotation
- shared typed UV
- typed face quad 生成

很容易一次把一堆已经基本收口的家族带坏。

开发原则应该是：

- 先能 family 内部修，就 family 内部修
- 只有明确证据表明 shared 层错了，才去动 shared 层

---

## 9. culling / 邻接裁面当前状态

### 9.1 当前已经建立 registry

`mesh.rs` 里现在已经不是散落 if/else，而是有一层 registry 语义：

- `BlockProperties`
- `CullingPolicy`

### 9.2 目前已接入的关键能力

- glass 的同类例外
- 非对称 glass full-block 与完整方块相邻规则
- preserve-neighbor-faces whitelist

### 9.3 当前白名单逻辑要特别小心

目前已经给以下族做过“保邻面”：

- redstone family 某些块
- hopper / chest / barrel
- coral family
- glass family（并带例外）

如果继续改这里，一定要先想清楚你是在改：

- 当前块自己的面
还是
- 邻居朝向它的那一面

最近用户多次强调过：

**“保邻面”不是“这些块自己永不裁面”**

这个语义不要再写错。

---

## 10. chest family 当前状态

### 10.1 现状

chest family 近期改动非常多，是当前仓库最容易误触的区域之一。

已经做过的方向包括：

- half-based
- canonical single/left/right
- local AABB
- lock 分离
- 局部平移
- UV 对调

### 10.2 目前开发建议

用户最近已经明确说过多次“先暂停 chest，不要再碰 chest family”。  
所以当前原则是：

- **不要顺手改 chest**
- 如果不是明确收到 chest 任务，不要动

---

## 11. barrel 当前状态

### 11.1 当前结论

barrel 不是 special path，已经接到 typed 主链。

### 11.2 最近收过的问题

最近主要修的是：

- `x=90` variants 的 face remap / cull 语义
- 顶/底面映射问题

### 11.3 当前开发建议

如果不是 barrel 专项任务，不要再顺手碰它。

---

## 12. piston family 当前状态

### 12.1 当前范围

最近集中处理的是静态活塞家族：

- `minecraft:piston`
- `minecraft:sticky_piston`
- `minecraft:piston_head`

### 12.2 moving_piston 当前状态

`moving_piston` 有现成入口：

- `moving_piston_template(...)`
- block override 路径

但如果没有真实样本 / fixture，不要假装已经修好。

### 12.3 当前最新阶段

静态活塞当前已经：

- 切进 typed `blockstate -> model -> quads`
- 做了 piston 专用 debug
- 正在按 family 自己的 UV/朝向语义继续细修

也就是说：

- 现在不要再回头重查“typed 接没接”
- 主要应盯 family 内部的侧面 UV / local orientation

---

## 13. 资源与外部依赖当前规则

### 13.1 固定资源思路

项目现在实际上采用的是：

- vanilla jar 负责 blockstate/model 主输入
- 外部资源包只负责 texture overlay / fallback / 某些专题显示资源

### 13.2 当前开发时不要乱改的原则

如果不是明确任务要求，不要随便：

- 更换 blockstate/model 来源
- 让 overlay 资源参与 model 解析
- 把特殊资源源头混成新的通用主输入

---

## 14. 当前最重要的 fixture / 验证资产

根目录现在这些文件很重要：

### 14.1 piston

- `_full_mode_piston_fixture.litematic`
- `_full_mode_piston_complex_fixture.litematic`

### 14.2 预览图

预览图是程序运行结果，适合本机人工验收；固定保留的是 fixture 和 layout 文本。

如果你要改某个块族，最优先的顺序是：

1. 先找对应 fixture
2. 再生成本轮临时预览图
3. 然后只修那一族

---

## 15. 调试开关现状

当前项目已经散布了一些非常实用的 env debug 开关。

### 15.1 barrel

- `LBA_FULL_MODE_V2_BARREL_DEBUG=1`

### 15.2 chest

- `LBA_FULL_MODE_V2_CHEST_DEBUG=1`

### 15.3 piston

- `LBA_FULL_MODE_V2_PISTON_DEBUG=1`

### 15.4 redstone multipart

- `LBA_FULL_MODE_V2_MULTIPART_DEBUG=1`

### 15.5 rail

- `LBA_FULL_MODE_V2_RAIL_DEBUG=1`

### 15.6 typed block generic

- `LBA_FULL_MODE_V2_TYPED_DEBUG=1`

### 15.7 unknown block skip

- `LBA_FULL_MODE_V2_SKIP_DEBUG=1`

开发建议：

- 默认保持安静
- 只在修某一族时开启对应 debug
- 尽量不要把 debug 再做成全局噪声

---

## 16. 运行与构建方式

### 16.1 构建 viewer-core

```powershell
cd <repo>\tools\viewer-core
cargo build --release --bin litematica_native_viewer
```

### 16.2 同步到运行入口

```powershell
Copy-Item `
  '<repo>\tools\viewer-core\target\release\litematica_native_viewer.exe' `
  '<repo>\bin\viewer-backend\litematica_native_viewer.exe' `
  -Force
```

### 16.3 运行完整模式

```powershell
cd <repo>
.\bin\viewer-backend\litematica_native_viewer.exe "<file.litematic>" --chunk-size=32 --display-mode=full
```

### 16.4 输出预览图

```powershell
.\bin\viewer-backend\litematica_native_viewer.exe <file> --chunk-size=32 --display-mode=full --preview-output="<png>" --auto-exit-seconds=6
```

### 16.5 probe-scene

```powershell
.\bin\viewer-backend\litematica_native_viewer.exe <file> --chunk-size=32 --display-mode=full --probe-scene
```

---

## 17. 继续开发时最重要的边界原则

### 17.1 一次只修一个块族

这是当前仓库最重要的工程原则之一。

为什么：

- Full Mode V2 现在已经进入“块族逐步收口”阶段
- 公共层一动，往往多个块族都会跟着变

所以当前应该坚持：

- 一轮只修一个族
- 不发散
- 不顺手“顺便重构”

### 17.2 能在 family 内修，就不要去动 shared 层

尤其是这些层：

- shared rotation
- shared transform
- shared per-quad UV
- shared culling

它们现在已经服务很多块族了。

### 17.3 先用 fixture，再看大场景

如果一上来就拿大场景乱试：

- 很难定位问题
- 很容易误判“到底是哪一层错”

正确顺序应该是：

1. 最小 fixture
2. 专项 debug
3. verify png
4. 大场景回归

---

## 18. 当前最容易踩坑的点

### 18.1 把“邻居保面”写成“自己永不裁面”

这是最近已经踩过的坑。

### 18.2 把模板身份和世界落位写成一回事

chest 这边已经反复证明过，这么做很容易炸。

### 18.3 把 fixture 看起来“能显示”误判成“已经修好”

尤其是：

- moving_piston
- override 驱动块
- 侧面 UV 朝向问题

### 18.4 动 shared UV 层想一把梭

这通常会把：

- rail
- repeater
- hopper
- barrel
- piston

一起带坏。

### 18.5 把资源包当 model/blockstate 来源

当前资源规则是明确分层的，不要乱混。

---

## 19. 建议的后续开发顺序

按当前仓库真实状态，后续继续推进比较合理的顺序是：

1. 把静态 piston family 收干净
2. 只在有真实样本的前提下，再单独收 `moving_piston`
3. 继续补特殊块族，但一次一个
4. 最后再考虑更高层次的 shared 抽象整合

也就是说，现在不是“大重构时机”，而是“把 V2 已经切进去的块族逐个收口”的时机。

---

## 20. 总结

当前仓库已经不再是“早期原型”：

- `.litematic` 解析链是实的
- 三模式入口是通的
- Full Mode V2 是可运行、可出图、可逐块族收口的

但它也还没到“所有块都 fully done”的阶段。现在最需要的是：

- 严守边界
- 小步收族
- 少动 shared
- 多用 fixture 和 verify png

如果按这个节奏推进，仓库会继续稳定；如果频繁跨层大改，很容易把最近几周已经收掉的成果重新打乱。

