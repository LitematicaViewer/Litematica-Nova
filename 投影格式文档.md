# `.litematic` 投影格式文档

## 1. 文档目标

这份文档专门讲 `.litematic` 文件本身的内部结构，重点是：

- 它在文件层到底是什么
- 顶层 NBT 每一段的作用
- `Metadata`、`Regions`、`BlockStatePalette`、`BlockStates`、`TileEntities` 等字段分别干什么
- 为什么 `.litematic` 不是“直接存每个方块的字符串”
- 项目在解析时，实际是如何把这些数据还原成方块、区域、渲染输入的

这次不再把重点放在 UI 页面或完整模式链路，而是尽量把 `.litematic` 本身讲透。

---

## 2. `.litematic` 本质上是什么

`.litematic` 是 Litematica 模组使用的一种结构投影文件格式。它本质上是：

1. 一个 **GZip 压缩文件**
2. 解压后是一份 **NBT（Named Binary Tag）树**
3. 树里包含：
   - 顶层元数据
   - 一个或多个区域 `Regions`
   - 每个区域的方块状态 palette
   - 每个区域的压缩方块索引流
   - 实体和方块实体（tile entities / block entities）

所以从程序视角看，读取 `.litematic` 的第一步不是“解析 JSON”，而是：

- 打开文件
- 用 GZip 解压
- 再把内容按 NBT 反序列化成结构体

在本项目里，对应代码是：

- `tools/viewer-core/src/nbt.rs`

其中核心入口是：

```rust
pub fn load_litematic_root(path: &Path) -> Result<LitematicRoot>
```

它内部就是：

- `File::open(...)`
- `GzDecoder::new(file)`
- `fastnbt::from_reader(...)`

也就是说，`.litematic` 是“GZip 包起来的 NBT 树”。

---

## 3. 顶层结构总览

按本项目当前的解析结构，`.litematic` 顶层可以概括成下面这几个核心部分：

```text
LitematicRoot
├─ Metadata
├─ Regions
├─ Version
├─ SubVersion
└─ MinecraftDataVersion
```

在项目里的 Rust 结构体定义是：

```rust
pub struct LitematicRoot {
    pub metadata: MetadataNbt,
    pub regions: BTreeMap<String, RegionNbt>,
    pub version: i32,
    pub sub_version: Option<i32>,
    pub minecraft_data_version: Option<i32>,
}
```

这几个字段的含义如下：

- `Metadata`
  - 文件头级元信息
  - 用于作者、描述、尺寸、总方块数、预览图等
- `Regions`
  - 真正的空间内容主体
  - 每个子区域都是一个独立的小结构体
- `Version`
  - `.litematic` 文件格式版本
- `SubVersion`
  - 子版本号，可选
- `MinecraftDataVersion`
  - Minecraft 数据版本，用于 blockstate / model / 数据兼容判断

---

## 4. Metadata：文件头元数据

### 4.1 项目当前解析到的字段

在本项目里，`Metadata` 当前对应结构体：

```rust
pub struct MetadataNbt {
    pub author: Option<String>,
    pub description: Option<String>,
    pub name: Option<String>,
    pub total_blocks: Option<i32>,
    pub total_volume: Option<i32>,
    pub region_count: Option<i32>,
    pub enclosing_size: Option<EnclosingSize>,
}
```

也就是说，项目当前明确消费的 metadata 至少有：

- `Author`
- `Description`
- `Name`
- `TotalBlocks`
- `TotalVolume`
- `RegionCount`
- `EnclosingSize`

### 4.2 它们各自的用途

#### `Author`

作者字段。更偏展示用途，用于：

- 属性页显示
- 文件信息展示

#### `Description`

描述字段。也是展示用途，可能用于：

- 文件详情
- 属性页说明文字

#### `Name`

投影名称。和文件名不是一回事：

- 文件名是磁盘路径上的名字
- `Metadata.Name` 是文件内部记录的名字

#### `TotalBlocks`

投影内非空气方块总数。更偏统计展示。

#### `TotalVolume`

包围体体积，通常是外接包围盒体积，不等于非空气方块数。

#### `RegionCount`

区域数量。因为 `.litematic` 可以有多个 `Regions.<region_name>`。

#### `EnclosingSize`

整个投影的包围盒尺寸，通常形如：

```snbt
EnclosingSize: { x: ?, y: ?, z: ? }
```

它很关键，因为：

- UI 可以直接展示投影尺寸
- 渲染和统计可以据此快速判断整体规模

### 4.3 Metadata 里还有哪些常见字段

虽然当前项目的 `MetadataNbt` 没把所有可能字段都显式映射出来，但在 `.litematic` 生态里，Metadata 往往还可能包含：

- 创建时间
- 修改时间
- 预览图

本项目的设计文档也明确提到过：

- `PreviewImageData`
- `TimeCreated`
- `TimeModified`

因此可以把 Metadata 理解成：

- 当前项目“明确字段化”的一部分
- 加上“仍可能在文件里存在，但当前结构体未全部展开”的一部分

---

## 5. Version / SubVersion / MinecraftDataVersion

### 5.1 `Version`

这是 `.litematic` 文件格式本身的版本，不是 Minecraft 版本。

比如：

- 文件结构组织方式
- 某些字段的可用性

都可能和它有关。

### 5.2 `SubVersion`

某些文件会有子版本号，表示同一主版本下的细化差异。

### 5.3 `MinecraftDataVersion`

这个字段非常重要，它描述的是 Minecraft 数据版本，而不是文件封装版本。

它直接影响：

- 方块 ID 是否存在
- blockstate 属性名和属性值
- model / blockstate JSON 是否匹配

例如同一个块在不同数据版本里可能：

- 属性集合不同
- state 语义不同
- 资源路径不同

所以渲染链如果要正确解释 `.litematic`，必须知道它对应哪个数据版本。

---

## 6. Regions：真正的投影主体

### 6.1 Regions 是什么

`Regions` 是一个映射表：

```text
Regions
├─ <region_name_1> -> RegionNbt
├─ <region_name_2> -> RegionNbt
└─ ...
```

本项目对应结构体：

```rust
pub regions: BTreeMap<String, RegionNbt>
```

注意这里的 key 是 **区域名**，不是数字下标。

也就是说：

- 每个区域都有名字
- 名字本身就是结构的一部分
- 项目 UI 里的区域列表，本质上就是把这里的 key 列出来

### 6.2 为什么要分 Region

一个大的投影并不一定只能作为一个整体存储。分成多个 Region 的好处是：

- 可以表达多个相对独立的子结构
- 可以带各自的位置和尺寸
- 方便局部编辑、局部渲染、局部统计

---

## 7. RegionNbt 内部结构

本项目当前将每个区域解析成：

```rust
pub struct RegionNbt {
    pub position: Vec3i,
    pub size: Vec3i,
    pub block_state_palette: Vec<BlockStateNbt>,
    pub block_states: LongArray,
    #[serde(default)]
    pub entities: Vec<EntityNbt>,
    #[serde(default, rename = "TileEntities", alias = "BlockEntities")]
    pub tile_entities: Vec<Value>,
}
```

也就是一个 Region 至少包含：

- `Position`
- `Size`
- `BlockStatePalette`
- `BlockStates`
- `Entities`
- `TileEntities` / `BlockEntities`

下面逐个解释。

---

## 8. Position：区域相对位置

### 8.1 作用

`Position` 表示这个区域在整个投影坐标系中的基准位置。

结构是：

```rust
pub struct Vec3i {
    pub x: i32,
    pub y: i32,
    pub z: i32,
}
```

### 8.2 它不是“区域内方块坐标”

这里要分清两个层级：

- `Region.Position`
  - 这个区域整体放在世界/投影里的相对起点
- 区域内部存储索引
  - 某个方块在这个区域内的局部位置

最终某个方块的全局坐标，一般是：

`区域基准位置 + 区域内局部坐标`

### 8.3 为什么它重要

如果没有 `Position`，多个 Region 之间就没法拼成完整结构。

---

## 9. Size：区域尺寸

### 9.1 作用

`Size` 表示区域在 x/y/z 三个轴上的大小。

例如：

```snbt
Size: { x: 16, y: 10, z: 32 }
```

表示该区域体积是 `16 * 10 * 32` 个格子。

### 9.2 它和 `BlockStates` 长度直接相关

区域里有多少个“位置槽位”，是由 `Size` 决定的：

```rust
pub fn region_volume(size: &Vec3i) -> Result<usize>
```

本项目就是拿 `size` 算体积，然后去决定：

- 需要解多少个 palette index
- `BlockStates` 长数组理论长度应该是多少

### 9.3 负尺寸问题

这个项目当前显式处理了负尺寸：

- `region_volume` 用的是绝对值
- 还提供了：
  - `storage_to_region_coords(...)`
  - `region_coord_to_storage_coord(...)`

这说明 `.litematic` 的区域尺寸和坐标不能被简单理解成“永远正向从 0 增长”。

也就是说，某些区域可能是“反向定义”的：

- `Size.x < 0`
- `Size.y < 0`
- `Size.z < 0`

这种情况下：

- 文件里的存储顺序
- 程序里的区域坐标

之间需要做换算。

---

## 10. BlockStatePalette：状态字典

### 10.1 它是什么

`BlockStatePalette` 是一个数组，每一项都是一个“完整 blockstate”。

可以把它理解成：

```text
palette[0] = minecraft:air
palette[1] = minecraft:stone
palette[2] = minecraft:barrel[facing=east,open=true]
palette[3] = minecraft:redstone_wire[north=side,...]
...
```

### 10.2 为什么需要 palette

如果每个格子都直接存完整字符串：

- 文件会非常大
- 重复信息很多

所以 `.litematic` 采用和 Minecraft chunk 类似的思路：

- 先把唯一状态去重，放进 palette
- 再让每个格子只存一个 palette index

### 10.3 palette 和“方块”不是一回事

palette 不是“某种方块种类表”，而是 **完整状态表**。

比如同样是 barrel，也可能出现两个 palette 项：

- `minecraft:barrel[facing=north,open=false]`
- `minecraft:barrel[facing=east,open=true]`

因为这两个 blockstate 不同。

### 10.4 palette 在项目中的作用

项目里 palette 会进一步变成：

- scene 层 palette
- property_pool
- palette state key
- 完整模式的 `palette_keys`

也就是说，`.litematic` 的 palette 是整个后续渲染解释链的起点。

---

## 11. BlockStates：压缩索引流

### 11.1 它不直接存方块

`BlockStates` 不是一个“方块数组”，而是一个 **压缩过的 palette 索引流**。

本项目里它被解析成：

```rust
pub block_states: LongArray
```

也就是 NBT 的 `long[]`。

### 11.2 基本原理

区域中每个位置都要对应一个 palette index。

假设 palette 长度是 5，那么理论上每个格子只需要几位 bit 就够了，不需要用一个完整 int。

所以 `.litematic` 会：

1. 先算出存一个 palette index 需要多少 bit
2. 把所有格子的 index 按 bit 紧密打包进 `long[]`

### 11.3 本项目如何算 bit 数

对应函数：

```rust
pub fn bits_for_palette(palette_len: usize) -> usize
```

规则是：

- palette 长度越大，需要的 bit 越多
- 最低也至少是 2 bit

### 11.4 本项目如何解某个位置的 palette index

关键函数有三个：

- `palette_index_at(...)`
- `for_each_palette_index(...)`
- `decode_palette_frequencies(...)`

它们都遵循同一套位运算逻辑：

1. 先计算某个格子 index 对应的起始 bit 偏移
2. 再算它落在 `long[]` 的哪一个或两个槽中
3. 用 mask 把该索引值提出来

### 11.5 为什么有时会跨两个 long

因为索引是按 bit 连续打包的，而不是按 long 对齐。

所以一个 palette index 可能：

- 完整落在一个 `long` 里
- 或者横跨两个 `long`

本项目这两种情况都显式处理了。

### 11.6 这就是 `.litematic` 最核心的“体积优化”

如果只看 `.litematic` 外表，会以为它只是“NBT 存了方块”。

实际上真正的大头是：

- `BlockStatePalette`
- `BlockStates`

这对组合。

---

## 12. 存储坐标与区域坐标

### 12.1 为什么需要专门换算

`BlockStates` 解出来的 index，本质上只是“区域体积里的第 N 个槽位”。

程序必须把这个一维索引重新转成：

- `(x, y, z)` 存储坐标
- 再转成真正的区域坐标

### 12.2 正负尺寸带来的复杂性

如果区域尺寸允许为负，那么：

- 存储坐标从 0 开始递增
- 真实区域坐标不一定也是 0 到正数

因此本项目提供了：

- `storage_to_region_coords(...)`
- `region_coord_to_storage_coord(...)`

这两个函数就是在解决：

- 文件存储布局
- 区域逻辑坐标

之间的映射。

---

## 13. Entities：普通实体

### 13.1 它是什么

`Entities` 是区域中的普通实体列表，例如：

- 掉落物
- 生物
- 其它非 block entity 的实体

### 13.2 在本项目里的地位

本项目目前对 `Entities` 的消费相对次要，更多是保留和统计支持。

相比之下，渲染链里更敏感的是 `TileEntities`。

---

## 14. TileEntities / BlockEntities：方块实体

### 14.1 它们是什么

这是附着在某些方块上的额外 NBT 数据。

常见例子：

- 告示牌文字
- 容器内容
- 命令方块指令
- 活塞移动状态

### 14.2 为什么字段名会有两个

本项目当前兼容：

- `TileEntities`
- `BlockEntities`

对应代码：

```rust
#[serde(default, rename = "TileEntities", alias = "BlockEntities")]
pub tile_entities: Vec<Value>
```

这说明：

- 不同来源或不同版本文件，字段名可能不同
- 项目解析时做了兼容别名

### 14.3 为什么这里用的是 `Vec<Value>`

因为 tile entity 的结构高度异构：

- sign 有 sign 的字段
- moving_piston 有 moving_piston 的字段
- 容器又有容器的字段

所以不像 Metadata / Region 那样容易用一个统一静态结构体完整描述。

项目在这里先用通用 NBT `Value` 接住，再按具体块类型做二次解析。

### 14.4 它在本项目中的重要性

对渲染来说，某些块如果没有 tile entity，就不完整：

- sign 没文字
- moving_piston 没 progress

所以 `.litematic` 文件中的 block entity 数据，不是边角料，而是某些方块正确显示的必要组成部分。

---

## 15. PreviewImageData

### 15.1 它是什么

`PreviewImageData` 是嵌在 `.litematic` 文件里的预览图数据。

它的作用主要是：

- 文件列表页快速显示缩略图
- 属性页显示内嵌预览

### 15.2 它和运行时 preview png 的区别

不要把下面两类东西混淆：

#### 文件内部的 `PreviewImageData`

- 属于 `.litematic` 文件本身
- 是原始格式的一部分

#### 程序运行后生成的 preview png

预览图是 viewer 的运行结果，不是 `.litematic` 文件内部字段。它们可以用于本机人工验收，但不属于投影格式本身。

---

## 16. 一个 Region 里如何还原出某个方块

这是理解 `.litematic` 的关键流程。

### 16.1 先有区域

选定某个 `Region`，拿到：

- `Position`
- `Size`
- `BlockStatePalette`
- `BlockStates`

### 16.2 算体积

根据 `Size` 算出该 Region 一共有多少格：

```text
volume = abs(size.x) * abs(size.y) * abs(size.z)
```

### 16.3 算每格需要多少 bit

根据 palette 长度算 `nbits`。

### 16.4 从 `BlockStates` 解出 palette index

对每个一维位置 `index`：

- 找到对应的 bit 区间
- 从 `long[]` 中提取值
- 得到 `palette_index`

### 16.5 从 palette 取出 blockstate

再用：

```text
block_state = block_state_palette[palette_index]
```

得到真正的：

- block id
- properties

### 16.6 再把一维 index 还原成三维位置

然后：

- index -> store_x/store_y/store_z
- store coords -> region coords
- region coords + region.position -> 全局位置

这时一个方块才真正完整：

- 它是什么状态
- 它在哪

---

## 17. 为什么 `.litematic` 适合大型投影

因为它在文件结构上做了三层压缩/去重：

### 17.1 Region 分块

允许结构按子区域组织。

### 17.2 Palette 去重

相同 blockstate 只存一份。

### 17.3 Bit-packed BlockStates

每个格子只用最少位数存 palette index。

这也是为什么大型投影如果直接存“每格一个完整字符串”，会大得离谱，但 `.litematic` 仍然可以保持较好的体积。

---

## 18. 本项目当前实际解析到的 `.litematic` 关键字段一览

按当前代码，至少已经明确结构化解析了这些：

### 顶层

- `Metadata`
- `Regions`
- `Version`
- `SubVersion`
- `MinecraftDataVersion`

### Metadata

- `Author`
- `Description`
- `Name`
- `TotalBlocks`
- `TotalVolume`
- `RegionCount`
- `EnclosingSize`

### Region

- `Position`
- `Size`
- `BlockStatePalette`
- `BlockStates`
- `Entities`
- `TileEntities` / `BlockEntities`

---

## 19. 本项目对 `.litematic` 的典型使用方式

### 19.1 属性读取

主要消费：

- Metadata
- Regions 的名称 / Size / Position

### 19.2 统计

主要消费：

- Regions
- Palette
- BlockStates

### 19.3 渲染

主要消费：

- block id
- properties
- tile entities

然后再进一步映射到：

- blockstate JSON
- model
- textures
- quads

所以 `.litematic` 文件里真正“影响画面”的核心并不是 Metadata，而是：

- `BlockStatePalette`
- `BlockStates`
- `TileEntities`

---

## 20. 设计与排错时的正确分层

如果以后继续查投影相关问题，建议始终分成这四层：

### 20.1 文件层

`.litematic` 本身是否能打开、能解压、能解析 NBT。

### 20.2 区域层

`Regions`、`Size`、`Position`、`BlockStatePalette`、`BlockStates` 是否一致。

### 20.3 状态层

某个方块是否被正确还原为：

- 正确 block id
- 正确 properties

### 20.4 渲染解释层

才是后面的：

- blockstate variant
- model
- UV
- cullface
- override

很多问题其实不是 `.litematic` 文件错了，而是后面的解释链错了。

---

## 21. 一个简化的 SNBT 心智模型

为了便于理解，可以把 `.litematic` 想成下面这种简化结构：

```snbt
{
  Metadata: {
    Name: "...",
    Author: "...",
    Description: "...",
    EnclosingSize: { x: ..., y: ..., z: ... },
    TotalBlocks: ...,
    TotalVolume: ...
  },
  Version: ...,
  SubVersion: ...,
  MinecraftDataVersion: ...,
  Regions: {
    "region_name": {
      Position: { x: ..., y: ..., z: ... },
      Size: { x: ..., y: ..., z: ... },
      BlockStatePalette: [
        { Name: "minecraft:air" },
        { Name: "minecraft:stone" },
        { Name: "minecraft:barrel", Properties: { facing: "east", open: "true" } }
      ],
      BlockStates: [L; ...],
      Entities: [ ... ],
      TileEntities: [ ... ]
    }
  }
}
```

注意：

- 这只是帮助理解的“形状”
- 实际字段完整性、顺序、类型细节以真实 NBT 为准

---

## 22. 结论

`.litematic` 文件最关键的不是“它是一个 Minecraft 投影文件”这句定义，而是下面这件事：

它把一个三维结构拆成了：

- 文件头元数据
- 多个区域
- 每个区域的状态字典
- 每个区域的压缩索引流
- 方块实体附加数据

其中真正决定“每个格子是什么”的核心组合是：

- `BlockStatePalette`
- `BlockStates`

真正决定“某些特殊块是否完整”的关键补充是：

- `TileEntities`

如果你以后要继续扩这个项目，或者排查某个投影为什么显示不对，最值得先盯住的也是这三块。

